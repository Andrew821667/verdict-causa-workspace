# Current.md - Verdict Causa

## Общая информация

- **Проект:** Verdict Causa
- **Версия институционального пакета:** `contracts-ru-v0@0.50.0`
- **Дата создания журнала:** 2026-07-23
- **Последнее обновление:** 2026-07-29

## Цель проекта

Создать проверяемую систему юридического анализа для российской правовой
системы. Система должна разделять доказанные факты, нормы, формальные выводы,
человеческую юридическую оценку и русское объяснение результата.

## Текущая стадия

- **Стадия:** разработка институционального слоя договорного права
- **Текущая задача:** выпущен пакет `contracts-ru-v0@0.50.0` — институт
  безвозмездного пользования (статьи 689–701 ГК РФ, глава 36) с интеграцией
  в Phase 0, русской спецификацией, артефактами и миграциями
- **Блокеры:** отсутствуют

## Технологический стек

- Python, Pydantic, Z3
- Pytest, Ruff
- JSON-артефакты с воспроизводимым replay
- GitHub Actions

## Архитектурные правила

- Каждый правовой блок имеет отдельные evidence, mapping, constraint model,
  benchmark, red-team и synthetic artifact.
- Неполные либо непроверенные факты отклоняются до формального решателя.
- Общие и специальные нормы исполняются последовательно и сверяются между собой.
- Формальный результат не подменяет судебную оценку.
- Юридически значимые документы и пользовательские объяснения создаются на русском.

## План

### Завершено

- [x] Базовая архитектура и Phase 0
- [x] Заключение, действительность и исполнение обязательств
- [x] Обеспечение, перемена лиц, изменение и прекращение
- [x] Ответственность и убытки
- [x] Общие правила купли-продажи, статьи 454-491 ГК РФ
- [x] Специальные правила поставки, статьи 506-524 ГК РФ
- [x] Пакет `contracts-ru-v0@0.17.0`

### Следующий этап

- [x] Выбрать следующий приоритет по критериям завершения Этапа 0
- [x] Реализовать контракт допуска и минимизации пилотных данных
- [x] Добавить первичное benchmark и red-team покрытие
- [x] Интегрировать пилотную репетицию в Phase 0 (отдельный пункт readiness `ws10-pilot-admission`)
- [x] Выпустить пакет `0.18.0` с миграциями `*-to-0.18.0` и русской спецификацией допуска
- [x] Реализовать институт действия договора во времени (статьи 425 и 433 ГК РФ)
- [x] Выпустить пакет `0.19.0` с миграциями `*-to-0.19.0` и русской спецификацией
- [x] Реализовать институт исковой давности (статьи 195–208 ГК РФ)
- [x] Выпустить пакет `0.20.0` с миграциями `*-to-0.20.0` и русской спецификацией
- [x] Реализовать институт толкования договора (статья 431 ГК РФ)
- [x] Выпустить пакет `0.21.0` с миграциями `*-to-0.21.0` и русской спецификацией
- [x] Реализовать институт формы сделки (статьи 158–165 и 434 ГК РФ)
- [x] Выпустить пакет `0.22.0` с миграциями `*-to-0.22.0` и русской спецификацией
- [x] Реализовать институт предварительного договора (статья 429 ГК РФ)
- [x] Выпустить пакет `0.23.0` с миграциями `*-to-0.23.0` и русской спецификацией
- [x] Реализовать институт договора в пользу третьего лица (статья 430 ГК РФ)
- [x] Выпустить пакет `0.24.0` с миграциями `*-to-0.24.0` и русской спецификацией
- [x] Реализовать институт публичного договора (статья 426 ГК РФ)
- [x] Выпустить пакет `0.25.0` с миграциями `*-to-0.25.0` и русской спецификацией
- [x] Реализовать институт договора присоединения (статья 428 ГК РФ)
- [x] Выпустить пакет `0.26.0` с миграциями `*-to-0.26.0` и русской спецификацией
- [x] Реализовать институт заверений об обстоятельствах (статья 431.2 ГК РФ)
- [x] Выпустить пакет `0.27.0` с миграциями `*-to-0.27.0` и русской спецификацией
- [x] Реализовать институт преддоговорной ответственности (статья 434.1 ГК РФ)
- [x] Выпустить пакет `0.28.0` с миграциями `*-to-0.28.0` и русской спецификацией
- [x] Реализовать институт опционных конструкций (статьи 429.2–429.3 ГК РФ)
- [x] Выпустить пакет `0.29.0` с миграциями `*-to-0.29.0` и русской спецификацией
- [x] Реализовать институт рамочного и абонентского договора (статьи 429.1 и 429.4 ГК РФ)
- [x] Выпустить пакет `0.30.0` с миграциями `*-to-0.30.0` и русской спецификацией
- [x] Реализовать институт свободы договора и цены (статьи 421–424 ГК РФ)
- [x] Выпустить пакет `0.31.0` с миграциями `*-to-0.31.0` и русской спецификацией
- [x] Реализовать институт обязательного заключения и торгов (статьи 445–449 ГК РФ)
- [x] Выпустить пакет `0.32.0` с миграциями `*-to-0.32.0` и русской спецификацией
- [x] Реализовать институт общих положений об обязательствах (статьи 307–308.3 ГК РФ)
- [x] Выпустить пакет `0.33.0` с миграциями `*-to-0.33.0` и русской спецификацией
- [x] Реализовать институт розничной купли-продажи (статьи 492–505 ГК РФ)
- [x] Выпустить пакет `0.34.0` с миграциями `*-to-0.34.0` и русской спецификацией
- [x] Реализовать институт поставки для государственных нужд (статьи 525–534 ГК РФ)
- [x] Выпустить пакет `0.35.0` с миграциями `*-to-0.35.0` и русской спецификацией
- [x] Реализовать институт контрактации (статьи 535–538 ГК РФ)
- [x] Выпустить пакет `0.36.0` с миграциями `*-to-0.36.0` и русской спецификацией
- [x] Реализовать институт энергоснабжения (статьи 539–548 ГК РФ)
- [x] Выпустить пакет `0.37.0` с миграциями `*-to-0.37.0` и русской спецификацией
- [x] Реализовать институт продажи недвижимости (статьи 549–558 ГК РФ)
- [x] Выпустить пакет `0.38.0` с миграциями `*-to-0.38.0` и русской спецификацией
- [x] Реализовать институт продажи предприятия (статьи 559–566 ГК РФ)
- [x] Выпустить пакет `0.39.0` с миграциями `*-to-0.39.0` и русской спецификацией
- [x] Реализовать институт мены (статьи 567–571 ГК РФ)
- [x] Выпустить пакет `0.40.0` с миграциями `*-to-0.40.0` и русской спецификацией
- [x] Реализовать институт дарения (статьи 572–582 ГК РФ)
- [x] Выпустить пакет `0.41.0` с миграциями `*-to-0.41.0` и русской спецификацией
- [x] Реализовать институт ренты и пожизненного содержания (статьи 583–605 ГК РФ)
- [x] Выпустить пакет `0.42.0` с миграциями `*-to-0.42.0` и русской спецификацией

## Правила работы

- Работать крупными законченными блоками.
- Важные юридические материалы писать на русском языке.
- Сохранять строгую трассируемость фактов и правовых источников.
- Не смешивать формальную проверку с окончательным судебным выводом.
- После каждого крупного блока запускать полный локальный QA и GitHub Actions.

## История изменений

### 2026-07-23 - Создан рабочий журнал

- Зафиксированы цель, стек, архитектурные правила и состояние после выпуска
  `contracts-ru-v0@0.17.0`.
- Следующим этапом назначен выбор и сквозная реализация нового правового института.

### 2026-07-23 - Спроектирован пилотный контур v1

- По критериям концепции переход ко второму институту отложен до доказательства
  работоспособности Этапа 0 на максимально реалистичном пилотном кейсе.
- Реализованы псевдонимный intake, законное основание обработки, минимизация,
  независимые согласования, ограничение хранения и fail-closed gate.
- Первичная проверка пропускает 6 допустимых маршрутов и блокирует 32 опасных.

### 2026-07-23 - Подготовлена передача другому агенту

- Разработка остановлена до интеграции pilot artifact в Phase 0.
- Черновик `0.18.0` подготовлен в ветке `agent/pilot-0.18-handoff`.
- Полная инструкция, инварианты и порядок завершения записаны в
  `AGENT_HANDOFF.md`.

### 2026-07-23 - Завершён пилотный контур и выпущен `0.18.0`

- Пилотная репетиция интегрирована в Phase 0: в `src/causa/phase0/pipeline.py`
  добавлен отдельный пункт readiness `ws10-pilot-admission` со ссылками на
  репетиционный отчёт, benchmark `contracts-pilot-gate-benchmark-v1`, red-team
  `contracts-pilot-gate-red-team-v1` и utility-отчёт v1; migration-ссылки Phase 0
  переведены на target `0.18.0` c источником `0.17.0`; `ready_for_production=false`
  сохранён.
- Сгенерированы артефакты: `examples/synthetic_pilot_rehearsal_report.json`,
  16 отчётов миграции `*-to-0.18.0` (источники `0.1.0`, `0.3.0`, `0.4.0`–`0.17.0`);
  прежние отчёты `*-to-0.17.0` сохранены как исторические; затронутые артефакты
  пересобраны детерминированно.
- Создана русская спецификация `docs/pilot-data-admission-spec.md`; обновлены
  `README.md`, changelog, матрица совместимости, план Этапа 0, спецификация
  оценки и Red Team, описание первого института.
- Восстановлен зелёный CI: в `pyproject.toml` явно зафиксирован исторический
  набор правил ruff (`select = ["E4","E7","E9","F"]`), так как новая версия ruff
  расширяла набор по умолчанию и отмечала старые файлы вне текущего релиза.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` изменённого
  файла — чисто; `git diff --check` — чисто; `pytest` — `309 passed`;
  benchmark `6/6`, red-team `32/32`; pilot JSON воспроизводим, без сырого текста
  и прямых идентификаторов.
- Настоящий пилот на данных заказчика остаётся условием снятия
  `ready_for_production=false`.

### 2026-07-24 - Институт действия договора во времени и выпуск `0.19.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 425 и 433 ГК РФ: `src/causa/institutional/contracts/temporal_effect.py`
  (момент заключения консенсуального, реального и регистрируемого договора,
  вступление в силу, отлагательное вступление, обратное действие, истечение срока
  и сохранение ответственности за нарушение).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и согласованности момента заключения с formation; добавлен шаг
  `evaluate-contract-temporal-effect` в Phase 0 и ссылки в readiness (`ws4`, `ws8`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-temporal-effect-spec.md`.
- Версия пакета поднята до `0.19.0`; добавлены запись совместимости, шаг миграции
  `0.18.0 → 0.19.0`, фикстура `0.18.0-phase0-trace` и 17 отчётов `*-to-0.19.0`;
  прежние отчёты `*-to-0.17.0` и `*-to-0.18.0` сохранены как исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `git diff --check` — чисто; `pytest` — `319 passed`; артефакты и миграции
  воспроизводимы.

### 2026-07-24 - Институт исковой давности и выпуск `0.20.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 195–208 ГК РФ: `src/causa/institutional/contracts/limitation.py`
  (начало течения, общий и специальный срок, предельный десятилетний срок,
  приостановление, перерыв признанием долга, период судебной защиты, заявление
  стороны, восстановление срока, дополнительные требования и исключения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-limitation-period` в
  Phase 0 и ссылки в readiness (`ws4`, `ws8`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-limitation-spec.md`.
- Версия пакета поднята до `0.20.0`; добавлены запись совместимости, шаг
  миграции `0.19.0 → 0.20.0`, фикстура `0.19.0-phase0-trace` и 18 отчётов
  `*-to-0.20.0`; прежние отчёты `*-to-0.17.0`, `*-to-0.18.0` и `*-to-0.19.0`
  сохранены как исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `git diff --check` — чисто; `pytest` — `328 passed`; benchmark `10/10`,
  red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-24 - Институт толкования договора и выпуск `0.21.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 431 ГК РФ: `src/causa/institutional/contracts/interpretation.py`
  (буквальное толкование, сопоставление с другими условиями и смыслом договора
  в целом, действительная общая воля сторон с учётом обстоятельств, толкование
  против стороны, подготовившей условие).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-contract-interpretation`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-interpretation-spec.md`.
- Версия пакета поднята до `0.21.0`; добавлены запись совместимости, шаг
  миграции `0.20.0 → 0.21.0`, фикстура `0.20.0-phase0-trace` и 19 отчётов
  `*-to-0.21.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.20.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `git diff --check` — чисто; `pytest` — `337 passed`; benchmark `10/10`,
  red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-24 - Институт формы сделки и выпуск `0.22.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 158–165 и 434 ГК РФ: `src/causa/institutional/contracts/form.py`
  (требуемая форма — устная, простая письменная, нотариальная; допустимые
  способы соблюдения письменной формы — подписанный сторонами документ, обмен
  документами, действительная электронная подпись; последствия несоблюдения —
  лишение права ссылаться на свидетельские показания и ничтожность сделки).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-transaction-form`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-form-spec.md`.
- Версия пакета поднята до `0.22.0`; добавлены запись совместимости, шаг
  миграции `0.21.0 → 0.22.0`, фикстура `0.21.0-phase0-trace` и 20 отчётов
  `*-to-0.22.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.21.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `346 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт предварительного договора и выпуск `0.23.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 429 и пункту 4 статьи 445 ГК РФ:
  `src/causa/institutional/contracts/preliminary.py` (заключение и форма
  предварительного договора, определённость предмета основного договора,
  согласование спорных условий, срок заключения, уклонение стороны, понуждение
  к заключению и возмещение убытков, прекращение обязательств).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-preliminary-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-preliminary-spec.md`.
- Версия пакета поднята до `0.23.0`; добавлены запись совместимости, шаг
  миграции `0.22.0 → 0.23.0`, фикстура `0.22.0-phase0-trace` и 21 отчёт
  `*-to-0.23.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.22.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `355 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт договора в пользу третьего лица и выпуск `0.24.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 430 ГК РФ: `src/causa/institutional/contracts/third_party.py`
  (заключение и действительность договора в пользу третьего лица, право третьего
  лица требовать исполнения, связанность сторон после выражения намерения,
  переход права к кредитору при отказе третьего лица).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-third-party-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-third-party-spec.md`.
- Версия пакета поднята до `0.24.0`; добавлены запись совместимости, шаг
  миграции `0.23.0 → 0.24.0`, фикстура `0.23.0-phase0-trace` и 22 отчёта
  `*-to-0.24.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.23.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `364 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт публичного договора и выпуск `0.25.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 426 ГК РФ: `src/causa/institutional/contracts/public_contract.py`
  (обязанность заключить договор с каждым обратившимся, недопустимость
  необоснованного отказа и предпочтения, единство условий для соответствующей
  категории, понуждение к заключению и ничтожность противоречащих условий).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-public-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-public-spec.md`.
- Версия пакета поднята до `0.25.0`; добавлены запись совместимости, шаг
  миграции `0.24.0 → 0.25.0`, фикстура `0.24.0-phase0-trace` и 23 отчёта
  `*-to-0.25.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.24.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `373 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт договора присоединения и выпуск `0.26.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 428 ГК РФ: `src/causa/institutional/contracts/adhesion.py` (режим
  договора присоединения, распространение при явном неравенстве переговорных
  возможностей, основания для изменения или расторжения и ограничение для
  присоединившегося предпринимателя, знавшего условия).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-adhesion-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-adhesion-spec.md`.
- Версия пакета поднята до `0.26.0`; добавлены запись совместимости, шаг
  миграции `0.25.0 → 0.26.0`, фикстура `0.25.0-phase0-trace` и 24 отчёта
  `*-to-0.26.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.25.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `382 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт заверений об обстоятельствах и выпуск `0.27.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 431.2 ГК РФ: `src/causa/institutional/contracts/representations.py`
  (недостоверное заверение, имеющее значение для договора, основание
  ответственности, возмещение убытков или неустойка, право на отказ от договора
  при существенном значении и оспаривание при обмане).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-representations`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-representations-spec.md`.
- Версия пакета поднята до `0.27.0`; добавлены запись совместимости, шаг
  миграции `0.26.0 → 0.27.0`, фикстура `0.26.0-phase0-trace` и 25 отчётов
  `*-to-0.27.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.26.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `391 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт преддоговорной ответственности и выпуск `0.28.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 434.1 ГК РФ: `src/causa/institutional/contracts/precontractual.py`
  (недобросовестное ведение и прекращение переговоров, нарушение
  конфиденциальности сведений, возмещение убытков независимо от заключения
  договора и ничтожность соглашений об ограничении такой ответственности).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-precontractual-liability`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-precontractual-spec.md`.
- Версия пакета поднята до `0.28.0`; добавлены запись совместимости, шаг
  миграции `0.27.0 → 0.28.0`, фикстура `0.27.0-phase0-trace` и 26 отчётов
  `*-to-0.28.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.27.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `400 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт опционных конструкций и выпуск `0.29.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 429.2 и 429.3 ГК РФ: `src/causa/institutional/contracts/option.py`
  (действительность безотзывной оферты, заключение основного договора акцептом
  в срок, прекращение опциона, передаваемость права, право требовать по
  опционному договору, прекращение по истечении срока и невозвратность платежа).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-option-constructions`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-option-spec.md`.
- Версия пакета поднята до `0.29.0`; добавлены запись совместимости, шаг
  миграции `0.28.0 → 0.29.0`, фикстура `0.28.0-phase0-trace` и 27 отчётов
  `*-to-0.29.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.28.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `409 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт рамочного и абонентского договора и выпуск `0.30.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 429.1 и 429.4 ГК РФ: `src/causa/institutional/contracts/framework.py`
  (определение общих условий рамочного договора, их конкретизация отдельными
  договорами или заявками, применение общих условий к неурегулированным
  отношениям, право абонента требовать исполнение и его обязанность вносить
  плату независимо от требования).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-framework-constructions`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-framework-spec.md`.
- Версия пакета поднята до `0.30.0`; добавлены запись совместимости, шаг
  миграции `0.29.0 → 0.30.0`, фикстура `0.29.0-phase0-trace` и 28 отчётов
  `*-to-0.30.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.29.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `418 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт свободы договора и цены и выпуск `0.31.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 421–424 ГК РФ: `src/causa/institutional/contracts/freedom.py`
  (свобода заключения договора, непоименованный и смешанный договор, определение
  условий по усмотрению сторон, соответствие императивным нормам, действие
  принятого после заключения закона, презумпция возмездности и определение цены).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-freedom-and-price`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-freedom-spec.md`.
- Версия пакета поднята до `0.31.0`; добавлены запись совместимости, шаг
  миграции `0.30.0 → 0.31.0`, фикстура `0.30.0-phase0-trace` и 29 отчётов
  `*-to-0.31.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.30.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `427 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт обязательного заключения и торгов и выпуск `0.32.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 445–449 ГК РФ: `src/causa/institutional/contracts/procedure.py`
  (обязанность заключить договор, понуждение обязанной стороны и убытки,
  определение спорных условий судом, заключение на торгах через протокол,
  уклонение победителя и недействительность торгов с нарушением правил).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-conclusion-procedure`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-procedure-spec.md`.
- Версия пакета поднята до `0.32.0`; добавлены запись совместимости, шаг
  миграции `0.31.0 → 0.32.0`, фикстура `0.31.0-phase0-trace` и 30 отчётов
  `*-to-0.32.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.31.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `436 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт общих положений об обязательствах и выпуск `0.33.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 307–308.3 ГК РФ: `src/causa/institutional/contracts/general_obligations.py`
  (понятие обязательства и право требовать исполнения, добросовестность,
  необязательность для третьих лиц, альтернативные и факультативные
  обязательства, исполнение в натуре и судебная неустойка).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-general-obligations`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-general-obligations-spec.md`.
- Версия пакета поднята до `0.33.0`; добавлены запись совместимости, шаг
  миграции `0.32.0 → 0.33.0`, фикстура `0.32.0-phase0-trace` и 31 отчёт
  `*-to-0.33.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.32.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `445 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт розничной купли-продажи и выпуск `0.34.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 492–505 ГК РФ: `src/causa/institutional/contracts/retail_sale.py`
  (публичность договора, форма и чек, обязанность информирования, права при
  ненадлежащем качестве, обмен товара надлежащего качества и разница в цене).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-retail-sale`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Розничная купля-продажа — второй, после поставки, специальный вид
  купли-продажи в рамках доказательства Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-retail-sale-spec.md`.
- Версия пакета поднята до `0.34.0`; добавлены запись совместимости, шаг
  миграции `0.33.0 → 0.34.0`, фикстура `0.33.0-phase0-trace` и 32 отчёта
  `*-to-0.34.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.33.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `454 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт поставки для государственных нужд и выпуск `0.35.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 525–534 ГК РФ: `src/causa/institutional/contracts/state_supply.py`
  (заключение государственного контракта и обязательность заключения, прикрепление
  покупателя, отказ покупателя, оплата по ценам контракта, поручительство
  заказчика и возмещение убытков поставщику).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-state-supply`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Поставка для государственных нужд — третий, после поставки и розничной
  купли-продажи, специальный вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-state-supply-spec.md`.
- Версия пакета поднята до `0.35.0`; добавлены запись совместимости, шаг
  миграции `0.34.0 → 0.35.0`, фикстура `0.34.0-phase0-trace` и 33 отчёта
  `*-to-0.35.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.34.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `463 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт контрактации и выпуск `0.36.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 535–538 ГК РФ: `src/causa/institutional/contracts/contractation.py`
  (квалификация договора контрактации, обязанность заготовителя принять продукцию
  и недопустимость отказа, возврат отходов переработки, обязанность производителя
  по количеству и ассортименту и ответственность производителя при наличии вины).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-contractation`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Контрактация — четвёртый, после поставки, розничной купли-продажи и поставки
  для государственных нужд, специальный вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-contractation-spec.md`.
- Версия пакета поднята до `0.36.0`; добавлены запись совместимости, шаг
  миграции `0.35.0 → 0.36.0`, фикстура `0.35.0-phase0-trace` и 34 отчёта
  `*-to-0.36.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.35.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `472 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт энергоснабжения и выпуск `0.37.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 539–548 ГК РФ: `src/causa/institutional/contracts/energy_supply.py`
  (квалификация энергоснабжения, количество и качество энергии, право на отказ
  от оплаты некачественной энергии, содержание сетей и режим потребления, оплата
  по данным учёта и правомерность перерыва подачи).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-energy-supply`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Энергоснабжение — пятый, после поставки, розничной купли-продажи, поставки для
  государственных нужд и контрактации, специальный вид купли-продажи в рамках
  Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-energy-supply-spec.md`.
- Версия пакета поднята до `0.37.0`; добавлены запись совместимости, шаг
  миграции `0.36.0 → 0.37.0`, фикстура `0.36.0-phase0-trace` и 35 отчётов
  `*-to-0.37.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.36.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `481 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт продажи недвижимости и выпуск `0.38.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 549–558 ГК РФ: `src/causa/institutional/contracts/real_estate_sale.py`
  (квалификация продажи недвижимости, письменная форма, регистрация перехода
  права, определённость предмета и цены, передача по акту и уклонение, средства
  защиты по качеству и перечень лиц при продаже жилого помещения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-real-estate-sale`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Продажа недвижимости — шестой, после поставки, розничной купли-продажи,
  поставки для государственных нужд, контрактации и энергоснабжения, специальный
  вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-real-estate-sale-spec.md`.
- Версия пакета поднята до `0.38.0`; добавлены запись совместимости, шаг
  миграции `0.37.0 → 0.38.0`, фикстура `0.37.0-phase0-trace` и 36 отчётов
  `*-to-0.38.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.37.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `490 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт продажи предприятия и выпуск `0.39.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 559–566 ГК РФ: `src/causa/institutional/contracts/enterprise_sale.py`
  (квалификация продажи предприятия, письменная форма и регистрация договора,
  удостоверение состава, права кредиторов и солидарная ответственность, передача
  по акту, регистрация перехода права, уменьшение цены и публичные интересы).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-enterprise-sale`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Продажа предприятия — седьмой, после поставки, розничной купли-продажи,
  поставки для государственных нужд, контрактации, энергоснабжения и продажи
  недвижимости, специальный вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-enterprise-sale-spec.md`.
- Версия пакета поднята до `0.39.0`; добавлены запись совместимости, шаг
  миграции `0.38.0 → 0.39.0`, фикстура `0.38.0-phase0-trace` и 37 отчётов
  `*-to-0.39.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.38.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `499 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт мены и выпуск `0.40.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 567–571 ГК РФ: `src/causa/institutional/contracts/barter.py`
  (квалификация мены, субсидиарные правила купли-продажи, равноценность и разница
  в цене, встречное исполнение, одновременный переход права и ответственность за
  изъятие товара третьим лицом).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-barter`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Мена — первый договор за пределами главы 30 о купле-продаже (глава 31), к
  которому правила о купле-продаже применяются субсидиарно.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-barter-spec.md`.
- Версия пакета поднята до `0.40.0`; добавлены запись совместимости, шаг
  миграции `0.39.0 → 0.40.0`, фикстура `0.39.0-phase0-trace` и 38 отчётов
  `*-to-0.40.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.39.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `508 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт дарения и выпуск `0.41.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 572–582 ГК РФ: `src/causa/institutional/contracts/gift.py`
  (квалификация дарения, притворность при встречном предоставлении, форма и
  ничтожность, запрещение и ограничения, отказ одаряемого, отмена дарения и
  пожертвование).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-gift`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Дарение — безвозмездный договор главы 32, следующий за меной за пределами
  купли-продажи.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-gift-spec.md`.
- Версия пакета поднята до `0.41.0`; добавлены запись совместимости, шаг
  миграции `0.40.0 → 0.41.0`, фикстура `0.40.0-phase0-trace` и 39 отчётов
  `*-to-0.41.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.40.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `517 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-29 - Закрытие форматного техдолга и усиление CI

- `ruff format` применён ко всей кодовой базе: 28 файлов, накопивших расхождение
  с каноническим форматом, приведены к нему; 316 файлов `src`, `tests` и
  `scripts` полностью соответствуют `ruff format --check`.
- Затронуты только пробелы и переносы строк: все 874 артефакта в `examples/`
  после полной перегенерации побайтово идентичны состоянию до форматирования
  (сверка по sha256), `pytest` — `598 passed` без изменений.
- CI усилен, чтобы расхождение не возникло снова:
  `ruff check` расширен с `src tests` до `src tests scripts`, добавлен
  отдельный шаг `ruff format --check src tests scripts`.
- Версия пакета не менялась: изменения не затрагивают юридические модели,
  схемы данных и выводы, поэтому это обслуживание, а не семантический выпуск.

### 2026-07-28 - Институт безвозмездного пользования и выпуск `0.50.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 689–701 ГК РФ: `src/causa/institutional/contracts/gratuitous_use.py`
  (квалификация, запрет передачи вещи инсайдеру организации, предоставление
  вещи с принадлежностями, ответственность за умышленно скрытые недостатки,
  права третьих лиц, содержание вещи, риск случайной гибели, досрочное
  расторжение, месячный срок извещения и сохранение прав при отчуждении).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-gratuitous-use`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Ссуда — безвозмездный аналог аренды; запрет передачи инсайдеру проверяется
  только при квалификации отношений как безвозмездного пользования.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-gratuitous-use-spec.md`.
- Открыта глава 36 ГК РФ «Безвозмездное пользование».
- Версия пакета поднята до `0.50.0`; добавлены запись совместимости, шаг
  миграции `0.49.0 → 0.50.0`, фикстура `0.49.0-phase0-trace` и 48 отчётов
  `*-to-0.50.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.49.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `598 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт найма жилого помещения и выпуск `0.49.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 671–688 ГК РФ: `src/causa/institutional/contracts/residential_lease.py`
  (квалификация, письменная форма, пригодность и изолированность помещения,
  обязанности наймодателя, нарушения нанимателя, одностороннее изменение платы,
  преимущественное право на новый срок, судебный порядок расторжения и срок
  для устранения нарушения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-residential-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Преимущественное право не применяется к краткосрочному найму до одного года;
  срок для устранения проверяется только при нарушении нанимателя.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-residential-lease-spec.md`.
- Открыта глава 35 ГК РФ «Наём жилого помещения» после завершённой главы 34.
- Версия пакета поднята до `0.49.0`; добавлены запись совместимости, шаг
  миграции `0.48.0 → 0.49.0`, фикстура `0.48.0-phase0-trace` и 47 отчётов
  `*-to-0.49.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.48.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `589 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт финансовой аренды и выпуск `0.48.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 665–670 ГК РФ: `src/causa/institutional/contracts/leasing.py`
  (квалификация, допустимость предмета лизинга, уведомление продавца,
  непередача предмета по вине лизингодателя, переход риска в момент передачи,
  прямые требования арендатора к продавцу и солидарная ответственность).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-leasing`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Солидарная ответственность проверяется только при выборе продавца
  лизингодателем; ответственность за просрочку — только при самой просрочке.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-leasing-spec.md`.
- Выпуск завершает главу 34 ГК РФ «Аренда»: общие положения (`0.43.0`), прокат
  (`0.44.0`), транспортные средства (`0.45.0`), здания и сооружения (`0.46.0`),
  предприятия (`0.47.0`) и финансовая аренда (`0.48.0`).
- Версия пакета поднята до `0.48.0`; добавлены запись совместимости, шаг
  миграции `0.47.0 → 0.48.0`, фикстура `0.47.0-phase0-trace` и 46 отчётов
  `*-to-0.48.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.47.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `580 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт аренды предприятий и выпуск `0.47.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 656–664 ГК РФ: `src/causa/institutional/contracts/enterprise_lease.py`
  (квалификация, форма одного документа, государственная регистрация,
  уведомление кредиторов и согласие на перевод долгов, акт передачи и
  подготовка за счёт арендодателя, право распоряжения ценностями, содержание
  предприятия и подготовка к возврату за счёт арендатора).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-enterprise-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Аренда предприятия — аренда имущественного комплекса для предпринимательской
  деятельности; перевод долгов проверяется только при такой квалификации.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-enterprise-lease-spec.md`.
- Версия пакета поднята до `0.47.0`; добавлены запись совместимости, шаг
  миграции `0.46.0 → 0.47.0`, фикстура `0.46.0-phase0-trace` и 45 отчётов
  `*-to-0.47.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.46.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `571 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт аренды зданий и сооружений и выпуск `0.46.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 650–655 ГК РФ: `src/causa/institutional/contracts/building_lease.py`
  (квалификация, форма одного документа, государственная регистрация при сроке
  не менее года, права на земельный участок, сохранение права пользования при
  смене собственника, существенное условие о плате, акты передачи и возврата).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-building-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Регистрация проверяется только при сроке не менее года, сохранение права
  пользования участком — только при смене его собственника.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-building-lease-spec.md`.
- Версия пакета поднята до `0.46.0`; добавлены запись совместимости, шаг
  миграции `0.45.0 → 0.46.0`, фикстура `0.45.0-phase0-trace` и 44 отчёта
  `*-to-0.46.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.45.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `562 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-27 - Институт аренды транспортных средств и выпуск `0.45.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 632–649 ГК РФ: `src/causa/institutional/contracts/vehicle_lease.py`
  (квалификация, письменная форма, преимущественное право, содержание и ремонт,
  услуги экипажа, расходы по эксплуатации, страхование, субаренда и
  ответственность за вред третьим лицам).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-vehicle-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Аренда транспортных средств делится на аренду с экипажем (статьи 632–641) и
  без экипажа (статьи 642–649); признак вида договора определяет сторону,
  несущую соответствующую обязанность.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-vehicle-lease-spec.md`.
- Версия пакета поднята до `0.45.0`; добавлены запись совместимости, шаг
  миграции `0.44.0 → 0.45.0`, фикстура `0.44.0-phase0-trace` и 43 отчёта
  `*-to-0.45.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.44.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `553 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-27 - Институт проката и выпуск `0.44.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 626–631 ГК РФ: `src/causa/institutional/contracts/rental.py`
  (квалификация проката, письменная форма, предельный срок, преимущественное
  право, недостатки и их устранение, возврат части платы, ремонт и запрет
  распоряжения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-rental`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Прокат — вид аренды с публичным договором для потребительских целей (§2 главы 34).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-rental-spec.md`.
- Версия пакета поднята до `0.44.0`; добавлены запись совместимости, шаг
  миграции `0.43.0 → 0.44.0`, фикстура `0.43.0-phase0-trace` и 42 отчёта
  `*-to-0.44.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.43.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `544 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-27 - Институт аренды (общие положения) и выпуск `0.43.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 606–625 ГК РФ: `src/causa/institutional/contracts/lease.py`
  (квалификация аренды, определённость объекта, форма и регистрация,
  ответственность за недостатки, права третьих лиц, субаренда, капитальный
  ремонт, расторжение, преимущественное право и улучшения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Аренда — возмездный договор о передаче имущества во временное владение и
  пользование (глава 34), открывающий крупную главу об аренде.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-lease-spec.md`.
- Версия пакета поднята до `0.43.0`; добавлены запись совместимости, шаг
  миграции `0.42.0 → 0.43.0`, фикстура `0.42.0-phase0-trace` и 41 отчёт
  `*-to-0.43.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.42.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `535 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт ренты и пожизненного содержания и выпуск `0.42.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 583–605 ГК РФ: `src/causa/institutional/contracts/annuity.py`
  (квалификация ренты, нотариальная форма, обеспечение и проценты за просрочку,
  выкуп постоянной ренты, расторжение пожизненной ренты и обременение имущества
  пожизненного содержания).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-annuity`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Рента и пожизненное содержание — возмездный алеаторный договор главы 33.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-annuity-spec.md`.
- Версия пакета поднята до `0.42.0`; добавлены запись совместимости, шаг
  миграции `0.41.0 → 0.42.0`, фикстура `0.41.0-phase0-trace` и 40 отчётов
  `*-to-0.42.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.41.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `526 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.
