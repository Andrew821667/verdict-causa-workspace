# Current.md - Verdict Causa

## Общая информация

- **Проект:** Verdict Causa
- **Версия институционального пакета:** `contracts-ru-v0@0.99.0`
- **Дата создания журнала:** 2026-07-23
- **Последнее обновление:** 2026-08-11

## Цель проекта

Создать проверяемую систему юридического анализа для российской правовой
системы. Система должна разделять доказанные факты, нормы, формальные выводы,
человеческую юридическую оценку и русское объяснение результата.

## Текущая стадия

- **Стадия:** разработка институционального слоя договорного права
- **Текущая задача:** выпущен пакет `contracts-ru-v0@0.99.0` — закрыт последний
  существенный пробел покрытия: глава 9.1 ГК РФ о решениях собраний
- **Блокеры:** отсутствуют

## Технологический стек

- Python, Pydantic, Z3
- Pytest, Ruff
- JSON-артефакты с воспроизводимым replay
- GitHub Actions

## Архитектурные правила

- Каждый правовой блок имеет отдельные evidence, mapping, constraint model,
  benchmark, red-team и synthetic artifact.
- Неполные либо непроверенные факты отклоняются до формального решателя.
- Общие и специальные нормы исполняются последовательно и сверяются между собой.
- Формальный результат не подменяет судебную оценку.
- Юридически значимые документы и пользовательские объяснения создаются на русском.

## План

### Завершено

- [x] Базовая архитектура и Phase 0
- [x] Заключение, действительность и исполнение обязательств
- [x] Обеспечение, перемена лиц, изменение и прекращение
- [x] Ответственность и убытки
- [x] Общие правила купли-продажи, статьи 454-491 ГК РФ
- [x] Специальные правила поставки, статьи 506-524 ГК РФ
- [x] Пакет `contracts-ru-v0@0.17.0`

### Следующий этап

- [x] Выбрать следующий приоритет по критериям завершения Этапа 0
- [x] Реализовать контракт допуска и минимизации пилотных данных
- [x] Добавить первичное benchmark и red-team покрытие
- [x] Интегрировать пилотную репетицию в Phase 0 (отдельный пункт readiness `ws10-pilot-admission`)
- [x] Выпустить пакет `0.18.0` с миграциями `*-to-0.18.0` и русской спецификацией допуска
- [x] Реализовать институт действия договора во времени (статьи 425 и 433 ГК РФ)
- [x] Выпустить пакет `0.19.0` с миграциями `*-to-0.19.0` и русской спецификацией
- [x] Реализовать институт исковой давности (статьи 195–208 ГК РФ)
- [x] Выпустить пакет `0.20.0` с миграциями `*-to-0.20.0` и русской спецификацией
- [x] Реализовать институт толкования договора (статья 431 ГК РФ)
- [x] Выпустить пакет `0.21.0` с миграциями `*-to-0.21.0` и русской спецификацией
- [x] Реализовать институт формы сделки (статьи 158–165 и 434 ГК РФ)
- [x] Выпустить пакет `0.22.0` с миграциями `*-to-0.22.0` и русской спецификацией
- [x] Реализовать институт предварительного договора (статья 429 ГК РФ)
- [x] Выпустить пакет `0.23.0` с миграциями `*-to-0.23.0` и русской спецификацией
- [x] Реализовать институт договора в пользу третьего лица (статья 430 ГК РФ)
- [x] Выпустить пакет `0.24.0` с миграциями `*-to-0.24.0` и русской спецификацией
- [x] Реализовать институт публичного договора (статья 426 ГК РФ)
- [x] Выпустить пакет `0.25.0` с миграциями `*-to-0.25.0` и русской спецификацией
- [x] Реализовать институт договора присоединения (статья 428 ГК РФ)
- [x] Выпустить пакет `0.26.0` с миграциями `*-to-0.26.0` и русской спецификацией
- [x] Реализовать институт заверений об обстоятельствах (статья 431.2 ГК РФ)
- [x] Выпустить пакет `0.27.0` с миграциями `*-to-0.27.0` и русской спецификацией
- [x] Реализовать институт преддоговорной ответственности (статья 434.1 ГК РФ)
- [x] Выпустить пакет `0.28.0` с миграциями `*-to-0.28.0` и русской спецификацией
- [x] Реализовать институт опционных конструкций (статьи 429.2–429.3 ГК РФ)
- [x] Выпустить пакет `0.29.0` с миграциями `*-to-0.29.0` и русской спецификацией
- [x] Реализовать институт рамочного и абонентского договора (статьи 429.1 и 429.4 ГК РФ)
- [x] Выпустить пакет `0.30.0` с миграциями `*-to-0.30.0` и русской спецификацией
- [x] Реализовать институт свободы договора и цены (статьи 421–424 ГК РФ)
- [x] Выпустить пакет `0.31.0` с миграциями `*-to-0.31.0` и русской спецификацией
- [x] Реализовать институт обязательного заключения и торгов (статьи 445–449 ГК РФ)
- [x] Выпустить пакет `0.32.0` с миграциями `*-to-0.32.0` и русской спецификацией
- [x] Реализовать институт общих положений об обязательствах (статьи 307–308.3 ГК РФ)
- [x] Выпустить пакет `0.33.0` с миграциями `*-to-0.33.0` и русской спецификацией
- [x] Реализовать институт розничной купли-продажи (статьи 492–505 ГК РФ)
- [x] Выпустить пакет `0.34.0` с миграциями `*-to-0.34.0` и русской спецификацией
- [x] Реализовать институт поставки для государственных нужд (статьи 525–534 ГК РФ)
- [x] Выпустить пакет `0.35.0` с миграциями `*-to-0.35.0` и русской спецификацией
- [x] Реализовать институт контрактации (статьи 535–538 ГК РФ)
- [x] Выпустить пакет `0.36.0` с миграциями `*-to-0.36.0` и русской спецификацией
- [x] Реализовать институт энергоснабжения (статьи 539–548 ГК РФ)
- [x] Выпустить пакет `0.37.0` с миграциями `*-to-0.37.0` и русской спецификацией
- [x] Реализовать институт продажи недвижимости (статьи 549–558 ГК РФ)
- [x] Выпустить пакет `0.38.0` с миграциями `*-to-0.38.0` и русской спецификацией
- [x] Реализовать институт продажи предприятия (статьи 559–566 ГК РФ)
- [x] Выпустить пакет `0.39.0` с миграциями `*-to-0.39.0` и русской спецификацией
- [x] Реализовать институт мены (статьи 567–571 ГК РФ)
- [x] Выпустить пакет `0.40.0` с миграциями `*-to-0.40.0` и русской спецификацией
- [x] Реализовать институт дарения (статьи 572–582 ГК РФ)
- [x] Выпустить пакет `0.41.0` с миграциями `*-to-0.41.0` и русской спецификацией
- [x] Реализовать институт ренты и пожизненного содержания (статьи 583–605 ГК РФ)
- [x] Выпустить пакет `0.42.0` с миграциями `*-to-0.42.0` и русской спецификацией

## Правила работы

- Работать крупными законченными блоками.
- Важные юридические материалы писать на русском языке.
- Сохранять строгую трассируемость фактов и правовых источников.
- Не смешивать формальную проверку с окончательным судебным выводом.
- После каждого крупного блока запускать полный локальный QA и GitHub Actions.

## История изменений

### 2026-07-23 - Создан рабочий журнал

- Зафиксированы цель, стек, архитектурные правила и состояние после выпуска
  `contracts-ru-v0@0.17.0`.
- Следующим этапом назначен выбор и сквозная реализация нового правового института.

### 2026-07-23 - Спроектирован пилотный контур v1

- По критериям концепции переход ко второму институту отложен до доказательства
  работоспособности Этапа 0 на максимально реалистичном пилотном кейсе.
- Реализованы псевдонимный intake, законное основание обработки, минимизация,
  независимые согласования, ограничение хранения и fail-closed gate.
- Первичная проверка пропускает 6 допустимых маршрутов и блокирует 32 опасных.

### 2026-07-23 - Подготовлена передача другому агенту

- Разработка остановлена до интеграции pilot artifact в Phase 0.
- Черновик `0.18.0` подготовлен в ветке `agent/pilot-0.18-handoff`.
- Полная инструкция, инварианты и порядок завершения записаны в
  `AGENT_HANDOFF.md`.

### 2026-07-23 - Завершён пилотный контур и выпущен `0.18.0`

- Пилотная репетиция интегрирована в Phase 0: в `src/causa/phase0/pipeline.py`
  добавлен отдельный пункт readiness `ws10-pilot-admission` со ссылками на
  репетиционный отчёт, benchmark `contracts-pilot-gate-benchmark-v1`, red-team
  `contracts-pilot-gate-red-team-v1` и utility-отчёт v1; migration-ссылки Phase 0
  переведены на target `0.18.0` c источником `0.17.0`; `ready_for_production=false`
  сохранён.
- Сгенерированы артефакты: `examples/synthetic_pilot_rehearsal_report.json`,
  16 отчётов миграции `*-to-0.18.0` (источники `0.1.0`, `0.3.0`, `0.4.0`–`0.17.0`);
  прежние отчёты `*-to-0.17.0` сохранены как исторические; затронутые артефакты
  пересобраны детерминированно.
- Создана русская спецификация `docs/pilot-data-admission-spec.md`; обновлены
  `README.md`, changelog, матрица совместимости, план Этапа 0, спецификация
  оценки и Red Team, описание первого института.
- Восстановлен зелёный CI: в `pyproject.toml` явно зафиксирован исторический
  набор правил ruff (`select = ["E4","E7","E9","F"]`), так как новая версия ruff
  расширяла набор по умолчанию и отмечала старые файлы вне текущего релиза.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` изменённого
  файла — чисто; `git diff --check` — чисто; `pytest` — `309 passed`;
  benchmark `6/6`, red-team `32/32`; pilot JSON воспроизводим, без сырого текста
  и прямых идентификаторов.
- Настоящий пилот на данных заказчика остаётся условием снятия
  `ready_for_production=false`.

### 2026-07-24 - Институт действия договора во времени и выпуск `0.19.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 425 и 433 ГК РФ: `src/causa/institutional/contracts/temporal_effect.py`
  (момент заключения консенсуального, реального и регистрируемого договора,
  вступление в силу, отлагательное вступление, обратное действие, истечение срока
  и сохранение ответственности за нарушение).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и согласованности момента заключения с formation; добавлен шаг
  `evaluate-contract-temporal-effect` в Phase 0 и ссылки в readiness (`ws4`, `ws8`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-temporal-effect-spec.md`.
- Версия пакета поднята до `0.19.0`; добавлены запись совместимости, шаг миграции
  `0.18.0 → 0.19.0`, фикстура `0.18.0-phase0-trace` и 17 отчётов `*-to-0.19.0`;
  прежние отчёты `*-to-0.17.0` и `*-to-0.18.0` сохранены как исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `git diff --check` — чисто; `pytest` — `319 passed`; артефакты и миграции
  воспроизводимы.

### 2026-07-24 - Институт исковой давности и выпуск `0.20.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 195–208 ГК РФ: `src/causa/institutional/contracts/limitation.py`
  (начало течения, общий и специальный срок, предельный десятилетний срок,
  приостановление, перерыв признанием долга, период судебной защиты, заявление
  стороны, восстановление срока, дополнительные требования и исключения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-limitation-period` в
  Phase 0 и ссылки в readiness (`ws4`, `ws8`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-limitation-spec.md`.
- Версия пакета поднята до `0.20.0`; добавлены запись совместимости, шаг
  миграции `0.19.0 → 0.20.0`, фикстура `0.19.0-phase0-trace` и 18 отчётов
  `*-to-0.20.0`; прежние отчёты `*-to-0.17.0`, `*-to-0.18.0` и `*-to-0.19.0`
  сохранены как исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `git diff --check` — чисто; `pytest` — `328 passed`; benchmark `10/10`,
  red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-24 - Институт толкования договора и выпуск `0.21.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 431 ГК РФ: `src/causa/institutional/contracts/interpretation.py`
  (буквальное толкование, сопоставление с другими условиями и смыслом договора
  в целом, действительная общая воля сторон с учётом обстоятельств, толкование
  против стороны, подготовившей условие).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-contract-interpretation`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-interpretation-spec.md`.
- Версия пакета поднята до `0.21.0`; добавлены запись совместимости, шаг
  миграции `0.20.0 → 0.21.0`, фикстура `0.20.0-phase0-trace` и 19 отчётов
  `*-to-0.21.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.20.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `git diff --check` — чисто; `pytest` — `337 passed`; benchmark `10/10`,
  red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-24 - Институт формы сделки и выпуск `0.22.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 158–165 и 434 ГК РФ: `src/causa/institutional/contracts/form.py`
  (требуемая форма — устная, простая письменная, нотариальная; допустимые
  способы соблюдения письменной формы — подписанный сторонами документ, обмен
  документами, действительная электронная подпись; последствия несоблюдения —
  лишение права ссылаться на свидетельские показания и ничтожность сделки).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-transaction-form`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-form-spec.md`.
- Версия пакета поднята до `0.22.0`; добавлены запись совместимости, шаг
  миграции `0.21.0 → 0.22.0`, фикстура `0.21.0-phase0-trace` и 20 отчётов
  `*-to-0.22.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.21.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `346 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт предварительного договора и выпуск `0.23.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 429 и пункту 4 статьи 445 ГК РФ:
  `src/causa/institutional/contracts/preliminary.py` (заключение и форма
  предварительного договора, определённость предмета основного договора,
  согласование спорных условий, срок заключения, уклонение стороны, понуждение
  к заключению и возмещение убытков, прекращение обязательств).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-preliminary-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-preliminary-spec.md`.
- Версия пакета поднята до `0.23.0`; добавлены запись совместимости, шаг
  миграции `0.22.0 → 0.23.0`, фикстура `0.22.0-phase0-trace` и 21 отчёт
  `*-to-0.23.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.22.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `355 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт договора в пользу третьего лица и выпуск `0.24.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 430 ГК РФ: `src/causa/institutional/contracts/third_party.py`
  (заключение и действительность договора в пользу третьего лица, право третьего
  лица требовать исполнения, связанность сторон после выражения намерения,
  переход права к кредитору при отказе третьего лица).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-third-party-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-third-party-spec.md`.
- Версия пакета поднята до `0.24.0`; добавлены запись совместимости, шаг
  миграции `0.23.0 → 0.24.0`, фикстура `0.23.0-phase0-trace` и 22 отчёта
  `*-to-0.24.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.23.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `364 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт публичного договора и выпуск `0.25.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 426 ГК РФ: `src/causa/institutional/contracts/public_contract.py`
  (обязанность заключить договор с каждым обратившимся, недопустимость
  необоснованного отказа и предпочтения, единство условий для соответствующей
  категории, понуждение к заключению и ничтожность противоречащих условий).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-public-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-public-spec.md`.
- Версия пакета поднята до `0.25.0`; добавлены запись совместимости, шаг
  миграции `0.24.0 → 0.25.0`, фикстура `0.24.0-phase0-trace` и 23 отчёта
  `*-to-0.25.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.24.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `373 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт договора присоединения и выпуск `0.26.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 428 ГК РФ: `src/causa/institutional/contracts/adhesion.py` (режим
  договора присоединения, распространение при явном неравенстве переговорных
  возможностей, основания для изменения или расторжения и ограничение для
  присоединившегося предпринимателя, знавшего условия).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-adhesion-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-adhesion-spec.md`.
- Версия пакета поднята до `0.26.0`; добавлены запись совместимости, шаг
  миграции `0.25.0 → 0.26.0`, фикстура `0.25.0-phase0-trace` и 24 отчёта
  `*-to-0.26.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.25.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `382 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт заверений об обстоятельствах и выпуск `0.27.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 431.2 ГК РФ: `src/causa/institutional/contracts/representations.py`
  (недостоверное заверение, имеющее значение для договора, основание
  ответственности, возмещение убытков или неустойка, право на отказ от договора
  при существенном значении и оспаривание при обмане).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-representations`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-representations-spec.md`.
- Версия пакета поднята до `0.27.0`; добавлены запись совместимости, шаг
  миграции `0.26.0 → 0.27.0`, фикстура `0.26.0-phase0-trace` и 25 отчётов
  `*-to-0.27.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.26.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `391 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт преддоговорной ответственности и выпуск `0.28.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статье 434.1 ГК РФ: `src/causa/institutional/contracts/precontractual.py`
  (недобросовестное ведение и прекращение переговоров, нарушение
  конфиденциальности сведений, возмещение убытков независимо от заключения
  договора и ничтожность соглашений об ограничении такой ответственности).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-precontractual-liability`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-precontractual-spec.md`.
- Версия пакета поднята до `0.28.0`; добавлены запись совместимости, шаг
  миграции `0.27.0 → 0.28.0`, фикстура `0.27.0-phase0-trace` и 26 отчётов
  `*-to-0.28.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.27.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `400 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт опционных конструкций и выпуск `0.29.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 429.2 и 429.3 ГК РФ: `src/causa/institutional/contracts/option.py`
  (действительность безотзывной оферты, заключение основного договора акцептом
  в срок, прекращение опциона, передаваемость права, право требовать по
  опционному договору, прекращение по истечении срока и невозвратность платежа).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-option-constructions`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-option-spec.md`.
- Версия пакета поднята до `0.29.0`; добавлены запись совместимости, шаг
  миграции `0.28.0 → 0.29.0`, фикстура `0.28.0-phase0-trace` и 27 отчётов
  `*-to-0.29.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.28.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `409 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт рамочного и абонентского договора и выпуск `0.30.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 429.1 и 429.4 ГК РФ: `src/causa/institutional/contracts/framework.py`
  (определение общих условий рамочного договора, их конкретизация отдельными
  договорами или заявками, применение общих условий к неурегулированным
  отношениям, право абонента требовать исполнение и его обязанность вносить
  плату независимо от требования).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-framework-constructions`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-framework-spec.md`.
- Версия пакета поднята до `0.30.0`; добавлены запись совместимости, шаг
  миграции `0.29.0 → 0.30.0`, фикстура `0.29.0-phase0-trace` и 28 отчётов
  `*-to-0.30.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.29.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `418 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт свободы договора и цены и выпуск `0.31.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 421–424 ГК РФ: `src/causa/institutional/contracts/freedom.py`
  (свобода заключения договора, непоименованный и смешанный договор, определение
  условий по усмотрению сторон, соответствие императивным нормам, действие
  принятого после заключения закона, презумпция возмездности и определение цены).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-freedom-and-price`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-freedom-spec.md`.
- Версия пакета поднята до `0.31.0`; добавлены запись совместимости, шаг
  миграции `0.30.0 → 0.31.0`, фикстура `0.30.0-phase0-trace` и 29 отчётов
  `*-to-0.31.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.30.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `427 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт обязательного заключения и торгов и выпуск `0.32.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 445–449 ГК РФ: `src/causa/institutional/contracts/procedure.py`
  (обязанность заключить договор, понуждение обязанной стороны и убытки,
  определение спорных условий судом, заключение на торгах через протокол,
  уклонение победителя и недействительность торгов с нарушением правил).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-conclusion-procedure`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-procedure-spec.md`.
- Версия пакета поднята до `0.32.0`; добавлены запись совместимости, шаг
  миграции `0.31.0 → 0.32.0`, фикстура `0.31.0-phase0-trace` и 30 отчётов
  `*-to-0.32.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.31.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `436 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт общих положений об обязательствах и выпуск `0.33.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 307–308.3 ГК РФ: `src/causa/institutional/contracts/general_obligations.py`
  (понятие обязательства и право требовать исполнения, добросовестность,
  необязательность для третьих лиц, альтернативные и факультативные
  обязательства, исполнение в натуре и судебная неустойка).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-general-obligations`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-general-obligations-spec.md`.
- Версия пакета поднята до `0.33.0`; добавлены запись совместимости, шаг
  миграции `0.32.0 → 0.33.0`, фикстура `0.32.0-phase0-trace` и 31 отчёт
  `*-to-0.33.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.32.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `445 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт розничной купли-продажи и выпуск `0.34.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 492–505 ГК РФ: `src/causa/institutional/contracts/retail_sale.py`
  (публичность договора, форма и чек, обязанность информирования, права при
  ненадлежащем качестве, обмен товара надлежащего качества и разница в цене).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-retail-sale`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Розничная купля-продажа — второй, после поставки, специальный вид
  купли-продажи в рамках доказательства Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-retail-sale-spec.md`.
- Версия пакета поднята до `0.34.0`; добавлены запись совместимости, шаг
  миграции `0.33.0 → 0.34.0`, фикстура `0.33.0-phase0-trace` и 32 отчёта
  `*-to-0.34.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.33.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `454 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-25 - Институт поставки для государственных нужд и выпуск `0.35.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 525–534 ГК РФ: `src/causa/institutional/contracts/state_supply.py`
  (заключение государственного контракта и обязательность заключения, прикрепление
  покупателя, отказ покупателя, оплата по ценам контракта, поручительство
  заказчика и возмещение убытков поставщику).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-state-supply`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Поставка для государственных нужд — третий, после поставки и розничной
  купли-продажи, специальный вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-state-supply-spec.md`.
- Версия пакета поднята до `0.35.0`; добавлены запись совместимости, шаг
  миграции `0.34.0 → 0.35.0`, фикстура `0.34.0-phase0-trace` и 33 отчёта
  `*-to-0.35.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.34.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `463 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт контрактации и выпуск `0.36.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 535–538 ГК РФ: `src/causa/institutional/contracts/contractation.py`
  (квалификация договора контрактации, обязанность заготовителя принять продукцию
  и недопустимость отказа, возврат отходов переработки, обязанность производителя
  по количеству и ассортименту и ответственность производителя при наличии вины).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-contractation`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Контрактация — четвёртый, после поставки, розничной купли-продажи и поставки
  для государственных нужд, специальный вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-contractation-spec.md`.
- Версия пакета поднята до `0.36.0`; добавлены запись совместимости, шаг
  миграции `0.35.0 → 0.36.0`, фикстура `0.35.0-phase0-trace` и 34 отчёта
  `*-to-0.36.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.35.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `472 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт энергоснабжения и выпуск `0.37.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 539–548 ГК РФ: `src/causa/institutional/contracts/energy_supply.py`
  (квалификация энергоснабжения, количество и качество энергии, право на отказ
  от оплаты некачественной энергии, содержание сетей и режим потребления, оплата
  по данным учёта и правомерность перерыва подачи).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-energy-supply`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Энергоснабжение — пятый, после поставки, розничной купли-продажи, поставки для
  государственных нужд и контрактации, специальный вид купли-продажи в рамках
  Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-energy-supply-spec.md`.
- Версия пакета поднята до `0.37.0`; добавлены запись совместимости, шаг
  миграции `0.36.0 → 0.37.0`, фикстура `0.36.0-phase0-trace` и 35 отчётов
  `*-to-0.37.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.36.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `481 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт продажи недвижимости и выпуск `0.38.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 549–558 ГК РФ: `src/causa/institutional/contracts/real_estate_sale.py`
  (квалификация продажи недвижимости, письменная форма, регистрация перехода
  права, определённость предмета и цены, передача по акту и уклонение, средства
  защиты по качеству и перечень лиц при продаже жилого помещения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-real-estate-sale`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Продажа недвижимости — шестой, после поставки, розничной купли-продажи,
  поставки для государственных нужд, контрактации и энергоснабжения, специальный
  вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-real-estate-sale-spec.md`.
- Версия пакета поднята до `0.38.0`; добавлены запись совместимости, шаг
  миграции `0.37.0 → 0.38.0`, фикстура `0.37.0-phase0-trace` и 36 отчётов
  `*-to-0.38.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.37.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `490 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт продажи предприятия и выпуск `0.39.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 559–566 ГК РФ: `src/causa/institutional/contracts/enterprise_sale.py`
  (квалификация продажи предприятия, письменная форма и регистрация договора,
  удостоверение состава, права кредиторов и солидарная ответственность, передача
  по акту, регистрация перехода права, уменьшение цены и публичные интересы).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-enterprise-sale`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Продажа предприятия — седьмой, после поставки, розничной купли-продажи,
  поставки для государственных нужд, контрактации, энергоснабжения и продажи
  недвижимости, специальный вид купли-продажи в рамках Этапа 0.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-enterprise-sale-spec.md`.
- Версия пакета поднята до `0.39.0`; добавлены запись совместимости, шаг
  миграции `0.38.0 → 0.39.0`, фикстура `0.38.0-phase0-trace` и 37 отчётов
  `*-to-0.39.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.38.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `499 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт мены и выпуск `0.40.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 567–571 ГК РФ: `src/causa/institutional/contracts/barter.py`
  (квалификация мены, субсидиарные правила купли-продажи, равноценность и разница
  в цене, встречное исполнение, одновременный переход права и ответственность за
  изъятие товара третьим лицом).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-barter`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Мена — первый договор за пределами главы 30 о купле-продаже (глава 31), к
  которому правила о купле-продаже применяются субсидиарно.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-barter-spec.md`.
- Версия пакета поднята до `0.40.0`; добавлены запись совместимости, шаг
  миграции `0.39.0 → 0.40.0`, фикстура `0.39.0-phase0-trace` и 38 отчётов
  `*-to-0.40.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.39.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `508 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт дарения и выпуск `0.41.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 572–582 ГК РФ: `src/causa/institutional/contracts/gift.py`
  (квалификация дарения, притворность при встречном предоставлении, форма и
  ничтожность, запрещение и ограничения, отказ одаряемого, отмена дарения и
  пожертвование).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-gift`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Дарение — безвозмездный договор главы 32, следующий за меной за пределами
  купли-продажи.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-gift-spec.md`.
- Версия пакета поднята до `0.41.0`; добавлены запись совместимости, шаг
  миграции `0.40.0 → 0.41.0`, фикстура `0.40.0-phase0-trace` и 39 отчётов
  `*-to-0.41.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.40.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `517 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-11 - Решения собраний (глава 9.1), выпуск `0.99.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  главе 9.1 ГК РФ, статьи 181.1–181.5:
  `src/causa/institutional/contracts/meeting_decisions.py` (последствия решения
  для всех имевших право участвовать, кворум и большинство, основания
  ничтожности и оспоримости, две исцеляющие оговорки).
- **Закрыт последний существенный пробел покрытия**, в который попала реальная
  практика: дело 45-КГ23-2-К7 сослалось на статьи 181.3 и 181.5. Покрытие
  поднялось с 48 до 49 дел из 50; осталась статья 420 с объяснением.
- **Решение собрания не является сделкой:** глава 9.1 отделена от статей
  166–181. В карте покрытия статья 181 принадлежит модели недействительности
  сделок, статьи 181.1–181.5 — новой модели; обе принадлежности закреплены
  тестом.
- Разделены три исхода: решение не принято, решение ничтожно, решение оспоримо.
  Оспоримое решение остаётся обязательным для участников до признания его
  недействительным.
- Предел исцеляющих оговорок статьи 181.4 закреплён red-team: они снимают
  оспоримость, но не ничтожность.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-meeting-decisions`
  в Phase 0.
- Добавлены benchmark (`12/12`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-meeting-decisions-spec.md`.
- Версия пакета поднята до `0.99.0`; добавлены запись совместимости, шаг
  миграции `0.98.0 → 0.99.0`, фикстура `0.98.0-phase0-trace` и 97 отчётов
  `*-to-0.99.0`; прежние отчёты сохранены как исторические.
- QA: `ruff check src tests scripts` — чисто; `git diff --check` — чисто;
  `pytest` — `1090 passed`; benchmark `12/12`, red-team `10/10`; артефакты и
  миграции воспроизводимы.

### 2026-08-11 - Закрытие долга о двух способах обработки противоречий

- Закрыт долг, записанный в `docs/contract-general-consistency-spec.md`: один и
  тот же класс проблемы обрабатывался двумя несовместимыми способами — часть
  противоречий отвергала анализ английским исключением по одному за прогон,
  часть называлась по-русски и поднимала флаг экспертизы.
- **Принцип границы сформулирован:** отвергать нужно там, где решателю иначе
  пришлось бы молча выбрать одну из версий факта; называть и помечать флагом —
  там, где обе версии уживаются в анализе. Разница не в тяжести противоречия, а
  в том, придётся ли выбирать. Закреплено тестом на непересечение наборов.
- Добавлен слой `src/causa/institutional/contracts/fact_consistency.py`; все 41
  сверка фактов переведены на него в обоих местах дублирования — в валидаторе
  результата и в раннере анализа.
- Расхождения выдаются все сразу, по-русски, со ссылкой на статью ГК РФ, из
  которой следует ожидаемое значение; ошибка типизирована (`FactConsistencyError`
  несёт `mismatches` и `keys`). Порядок проверок больше не влияет на результат.
- Отвержение осталось отвержением: гарантии воспроизводимости не ослаблены.
- Проверки воспроизводимости артефакта и состава данных остаются немедленными:
  они относятся к целостности артефакта, а не к фактам дела.
- Удалён `CONTRACT_CONTRADICTION_TYPES` — 45 имён противоречий без единого
  потребителя. Правило то же, что в выпуске `0.97.0`: объявленное и не
  проверяемое читается как покрытие, которого нет.
- Создана русская спецификация `docs/fact-consistency-spec.md`; запись о
  непоследовательности в прежней спецификации заменена описанием границы.
- QA: `ruff check src tests scripts` — чисто; `git diff --check` — чисто;
  `pytest` — `1077 passed`; артефакты не изменились, выпуск пакета не требуется.

### 2026-08-10 - Возложение ответственности и просрочка сторон, выпуск `0.98.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 402–406 ГК РФ: `src/causa/institutional/contracts/attribution_delay.py`
  (действия работников должника, возложение исполнения на третье лицо и случаи
  переноса ответственности законом, вина кредитора и непринятие мер к уменьшению
  убытков, просрочка должника, утрата интереса кредитора, просрочка кредитора).
- **Институт появился по результату измерения, а не по плану разбора кодекса.**
  Статьи 402–406 лежали между моделью ответственности (333–401) и моделью
  средств защиты, берущей из главы 25 только 393 и 406.1; реальные суды
  сослались на 403 и 404 дважды на пятидесяти делах.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-attribution-delay`
  в Phase 0.
- Два вывода меняют судьбу требования: возложение исполнения на третье лицо не
  переносит ответственность, а просрочка кредитора снимает основание считать
  должника просрочившим. Размер ответственности модель не определяет.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-attribution-delay-spec.md`.
- Покрытие реальной практики поднялось с 46 до 48 дел из 50; непокрытыми
  остались статьи 181.3, 181.5 и 420, каждая с объяснением.
- Версия пакета поднята до `0.98.0`; добавлены запись совместимости, шаг
  миграции `0.97.0 → 0.98.0`, фикстура `0.97.0-phase0-trace` и 96 отчётов
  `*-to-0.98.0`; прежние отчёты сохранены как исторические.
- QA: `ruff check src tests scripts` — чисто; `git diff --check` — чисто;
  `pytest` — `1070 passed`; benchmark `10/10`, red-team `10/10`; артефакты и
  миграции воспроизводимы.

### 2026-08-10 - Приём базы судебной практики и прогон на реальных делах

- Получена первая выгрузка судебной практики: 50 дел Верховного Суда из обзоров,
  ветка `data/practice-base-export`, файлы `data/practice/cases.jsonl` и
  `data/practice/inventory.md`. Тексты сверены с первоисточником, персональных
  данных нет, сопоставление с предикатами модели выгружающей стороной не
  делалось.
- Чтение выгрузки вскрыло два дефекта приёмной стороны. Номера статей были
  объявлены целыми числами, и ссылки на статьи 181.3, 181.5, 327.1, 388.1 и
  393.1 отвергались как испорченные данные. Тематические метки в задании агенту
  названы прозой, а в `REQUESTED_TOPICS` — ключами, и половина меток не сошлась.
  Обе стороны расхождения внесены заданием, обе исправлены здесь.
- Добавлено измерение покрытия `practice_coverage.py`: 106 различных статей
  реальной практики против диапазонов институтов. Пять статей не покрыты никем
  (181.3, 181.5 — решения собраний; 403, 404 — ответственность за третьих лиц и
  вина кредитора; 420 — понятие договора), каждая обязана быть объяснена тестом.
- Измерение нашло переобъявление в самом пакете: `obligation_dynamics` заявляет
  статьи 382–419, а предикаты разбирают главы 24 и 26 без главы 25. Пока карта
  повторяла заявленный диапазон, статьи 403 и 404 считались покрытыми, хотя их
  не разбирает никто.
- Добавлен набор `real_case_scenarios.py` — третий источник ожиданий после
  текста кодекса и разъяснений Пленума: решения по конкретным делам. Переведено
  4 дела, сошлись все 4; пятое дело с окончательным исходом не переведено, и
  причина записана — спор разрешён по Закону об ОСАГО, вне моделируемых норм.
- Инвариант модели вещных прав опроверг мой перевод фабулы 5-КГ24-43-К2: неучёт
  защиты добросовестного приобретателя не может стоять при ненарушенных правилах
  виндикации. Проверка по праву показала, что права модель.
- Спецификации `docs/practice-coverage-spec.md` и
  `docs/real-case-scenarios-spec.md`; задание агенту дополнено точными
  значениями `topic_tags`, строковой записью статей и требованием дел с
  окончательным исходом.
- Добавлена предварительная сверка входов `anchor_preflight.py`: все
  расхождения с якорными выводами собираются за один проход вместо одного за
  прогон. Поведения анализа не меняет — это подготовка данных. Спецификация
  `docs/anchor-preflight-spec.md`.
- QA: `ruff check src tests` — чисто; `pytest` — `1058 passed`.

### 2026-08-07 - Достижимость сверок, выпуск `0.97.0`

- **Измерено:** из десяти объявленных межинституциональных сверок в полном
  конвейере могли сработать восемь. Две — «договор не заключён, но оценивается
  недействительность» и «договор не заключён, но расторгается» — перехватывались
  прежними жёсткими проверками входов, которые проверяют то же условие и
  отвергают анализ раньше.
- Обе удалены вместе с входами и полями оценки. Объявленная, но неспособная
  сработать проверка вводит в заблуждение не меньше, чем список типов без
  потребителей: это тот же дефект уровнем выше.
- Добавлен параметризованный тест `test_every_declared_conflict_fires_end_to_end`:
  каждая объявленная сверка обязана сработать через
  `run_reviewed_contract_analysis`. Прежний тест проверял лишь наличие поля.
- Первое измерение дало три «недостижимы», но одна оказалась дефектом самого
  измерения: триггер не приводил в согласие факты обязательства. После
  исправления сверка сработала. Записано, потому что едва не стало ложной
  находкой — третьей за серию.
- В спецификации зафиксирована известная непоследовательность: часть противоречий
  в данных рецензента отвергает анализ исключением на английском, часть —
  называется по-русски и поднимает флаг. Второй способ вернее, но перевод жёстких
  проверок в мягкие затрагивает гарантии воспроизводимости и не выполнялся.
- Версия пакета поднята до `0.97.0`; добавлены запись совместимости, шаг
  миграции `0.96.0 → 0.97.0`, фикстура `0.96.0-phase0-trace`, 95 отчётов
  `*-to-0.97.0`.
- QA: `ruff` — чисто; `pytest` — `1036 passed`.

### 2026-08-07 - Фабулы по позициям Верховного Суда, выпуск `0.96.0`

- Добавлен `src/causa/institutional/contracts/practice_scenarios.py` — шесть
  фабул, где ожидаемый итог взят из опубликованной правовой позиции ВС РФ:
  Пленум № 25 (пп. 1, 80, 123 и разъяснения о согласии по статьям 157.1
  и 173.1), Пленум № 49, Пленум ВС № 10 / ВАС № 22 (п. 39). Результат `6/6`.
- Это следующий уровень независимости ожиданий: система сверяется не с моим
  прочтением кодекса, а с тем, что суд уже сказал.
- Две фабулы проверяют **отсутствие** лишнего вывода: при отсутствии согласия
  система обязана остановиться на оспоримости и не снимать действие договора;
  из одной лишь недействительности не должна выводить опровержение титула.
  Обе прошли.
- **Ограничение зафиксировано в коде.** Сетевая политика среды разрешает поиск,
  но блокирует загрузку страниц: `WebFetch` отклонён для `consultant.ru`,
  `garant.ru`, `vsrf.ru`, `sudact.ru`, `legalacts.ru`, `pravo.gov.ru`,
  `government.ru` и даже `en.wikipedia.org`; `curl` получает 403 от шлюза.
  Материалы конкретных судебных дел недоступны — картотеки закрыты. Тексты
  позиций получены из выдачи поиска и постранично не сверены.
- Каждая фабула несёт поле `verification`, тест отклоняет фабулу, умалчивающую
  о степени проверенности источника. Неуверенность выдачи в номере пункта
  записана в фабуле, а не сглажена.
- Векторная база судебной практики проекта Контрактной системы находится на
  Mac mini и из облачного контейнера недоступна; каркас `PracticeScenario`
  готов принять дела, когда доступ появится.
- Версия пакета поднята до `0.96.0`; добавлены запись совместимости, шаг
  миграции `0.95.0 → 0.96.0`, фикстура `0.95.0-phase0-trace`, 94 отчёта
  `*-to-0.96.0` и спецификация `docs/practice-scenarios-spec.md`.
- QA: `ruff` — чисто; `pytest` — `1026 passed`.

### 2026-08-06 - Смоделированные фабулы дел, выпуск `0.95.0`

- Добавлен `src/causa/institutional/contracts/case_scenarios.py` — двенадцать
  фабул, каждая проходит через `run_reviewed_contract_analysis` целиком, а не
  через один институт. Ожидаемый итог выведен из ГК РФ **до** запуска системы и
  записан в `legal_basis_ru`. Результат: `12/12`.
- **Найден структурный дефект.** Три фабулы с вытеснением договорного эффекта не
  проходили вовсе: `sale_breach_established` строится из фактов и от
  действительности договора не зависит, а `breach_issue` — от существования
  обязанности; жёсткая сверка требовала равенства. Система не могла разобрать
  ничтожную сделку с уже состоявшимся исполнением — класс дел, где нужна
  реституция.
- Исправлено: равенство требуется только при существующей обязанности. При
  вытеснении эффекта правовую судьбу фактического вывода определяет слой общих
  положений (`institute_conclusions_displaced`,
  `breach_findings_without_effect`).
- **Измерен каскад согласованности:** чтобы смоделировать дело с вытеснённым
  эффектом, рецензент обязан согласованно отразить это в семи контрактах данных
  помимо порождающего института — около двадцати предикатов, 14 привязок к
  якорным выводам. Каждое расхождение отвергается отдельной проверкой, и
  рецензент узнаёт о них по одной за прогон.
- Сужена сверка формы: по пункту 1 статьи 162 ГК РФ порок простой письменной
  формы недействительности не влечёт, поэтому прежняя проверка давала ложное
  срабатывание.
- Версия пакета поднята до `0.95.0`; добавлены запись совместимости, шаг
  миграции `0.94.0 → 0.95.0`, фикстура `0.94.0-phase0-trace`, 93 отчёта
  `*-to-0.95.0` и спецификация `docs/case-scenarios-spec.md`.
- QA: `ruff` — чисто; `pytest` — `1015 passed`.

### 2026-08-06 - Сверка фактов между институтами, выпуск `0.94.0`

- **Измерено до исправления:** из десяти построенных межинституциональных
  противоречий все десять принимались без единого сигнала. При расхождении о
  недееспособности `persons` утверждал недееспособность, `invalidity` отрицал,
  слой общих положений молча выбирал первую версию и выдавал юристу уверенное
  «договор ничтожен».
- Добавлен `src/causa/institutional/contracts/general_consistency.py` — второй
  узел пакета без собственного контракта данных. В отличие от слоя общих
  положений, его входы — не выводы институтов, а сами проверенные **факты**.
- Проверяются десять пар предикатов, описывающих одно и то же обстоятельство:
  недееспособность (29 и 171), правоспособность юрлица (49 и 173), согласие
  попечителя (30 и 176), сделка малолетнего (28 и 172), необходимое согласие
  (157.1 и 173.1), объект вне оборота — нарушение закона и публичный интерес
  (129 и 168), недействительность и расторжение незаключённого договора
  (432, 166 и 450), соблюдение формы (432 и 158–162).
- Слой намеренно не выбирает версию и не отклоняет анализ: обе стороны
  противоречия внесены человеком, и у формальной модели нет основания предпочесть
  одну. Он называет расхождение и поднимает `requires_human_resolution`.
- Оживлена таксономия: `contradiction_taxonomy` объявлял 45 типов противоречий и
  не проверял ни одного — список не импортировался никуда, кроме перечня файлов.
  Новый набор `CROSS_INSTITUTE_CONTRADICTION_TYPES` закреплён тестом
  `test_every_declared_cross_institute_type_is_checked`.
- Три гипотезы о противоречиях отброшены как юридически неверные: статья 183
  против 174, трёхлетняя давность против годичной по оспоримым сделкам и
  равенство `acceptance_received` с моментом заключения (в демодéле договор
  заключён конклюдентными действиями — проверка давала ложное срабатывание на
  каждом деле).
- Версия пакета поднята до `0.94.0`; добавлены запись совместимости, шаг
  миграции `0.93.0 → 0.94.0`, фикстура `0.93.0-phase0-trace`, 92 отчёта
  `*-to-0.94.0`, шаг `check-cross-institute-consistency` в Phase 0 и
  спецификация `docs/contract-general-consistency-spec.md`.
- QA: `ruff` — чисто; `pytest` — `998 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Объекты гражданских прав, выпуск `0.93.0`

- Добавлен `src/causa/institutional/contracts/objects.py` — формальная модель на
  Z3 по статьям 128–152 ГК РФ (перечень объектов, оборотоспособность, деление
  вещей на недвижимые и движимые, неделимые и сложные вещи, главная вещь и
  принадлежность, плоды и доходы, деньги и ценные бумаги, нематериальные блага,
  защита чести, достоинства и деловой репутации).
- Объекты подключены к слою общих положений:
  `object_excluded_from_circulation` даёт вывод
  `restricted_object_voids_transaction` и снимает `contract_legally_effective`
  (статьи 129 и пункт 2 статьи 168 ГК РФ).
- Это второе основание ничтожности из общей части наряду с недееспособностью
  стороны (статья 171). Оба снимают действие договора независимо от судебного
  признания и оба отличаются от оспоримости по статье 173.1.
- Слой обновлён до
  `contracts-general-part-effects-articles-10-129-167-171-173-1-183-190-199-209-432-v7`.
- Проверено сквозным прогоном и закреплено тестом
  `test_object_excluded_from_circulation_voids_the_contract`.
- Версия пакета поднята до `0.93.0`; добавлены запись совместимости, шаг
  миграции `0.92.0 → 0.93.0`, фикстура `0.92.0-phase0-trace`, 91 отчёт
  `*-to-0.93.0`, шаг `evaluate-objects` в Phase 0 и спецификация
  `docs/contract-objects-spec.md`.
- QA: `ruff` — чисто; `pytest` — `990 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Правоспособность и дееспособность, выпуск `0.92.0`

- Добавлен `src/causa/institutional/contracts/persons.py` — формальная модель на
  Z3 по статьям 17–53 ГК РФ (правоспособность гражданина, объём дееспособности
  по возрасту, признание недееспособным, ограничение дееспособности и согласие
  попечителя, соглашения об ограничении правоспособности, правоспособность
  юридического лица и лицензии, государственная регистрация, действия органов).
- Лица подключены к слою общих положений: `party_lacks_capacity` даёт вывод
  `incapacity_voids_transaction` и снимает `contract_legally_effective`.
- Проведено различие между ничтожностью и оспоримостью: сделка недееспособного
  ничтожна (статья 171 ГК РФ) и лишает договор действия независимо от судебного
  признания, тогда как сделка без необходимого согласия лишь оспорима
  (статья 173.1) и действия договора не снимает. Тест
  `test_declared_incapacity_voids_the_contract_through_general_effects`
  проверяет обе стороны различия.
- Ограничение дееспособности (статья 30) в слой намеренно не поступает: такая
  сделка оспорима по статье 176 ГК РФ и оценивается юристом.
- Слой обновлён до
  `contracts-general-part-effects-articles-10-167-171-173-1-183-190-199-209-432-v6`.
- Версия пакета поднята до `0.92.0`; добавлены запись совместимости, шаг
  миграции `0.91.0 → 0.92.0`, фикстура `0.91.0-phase0-trace`, 90 отчётов
  `*-to-0.92.0`, шаг `evaluate-persons` в Phase 0 и спецификация
  `docs/contract-persons-spec.md`.
- QA: `ruff` — чисто; `pytest` — `980 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Исчисление сроков, выпуск `0.91.0`

- Добавлен `src/causa/institutional/contracts/terms.py` — формальная модель на
  Z3 по статьям 190–194 ГК РФ (определение срока, неизбежность события, начало
  течения, окончание, перенос с нерабочего дня, исчисление срока исковой
  давности, действия в последний день срока, час прекращения операций в
  организации, сдача письменных извещений в организацию связи).
- Сроки подключены к слою общих положений: `term_calculation_defective` снимает
  `limitation_bars_protection` и даёт вывод `limitation_conclusion_unreliable`.
- Это первая связь слоя, работающая **на ослабление**, а не на усиление вывода.
  Все прежние входы общей части добавляли последствия: вытесняли договор
  (статья 183), лишали защиты (статьи 10 и 199), делали сделку оспоримой
  (статья 173.1). Здесь общая часть отбирает у специального института уже
  сделанный им вывод, потому что тот опирается на неверно применённые правила
  исчисления. Флаг экспертизы сохраняется: недостоверный вывод не равен
  отсутствию проблемы.
- Слой обновлён до
  `contracts-general-part-effects-articles-10-167-173-1-183-190-199-209-432-v5`.
- Проверено сквозным прогоном и закреплено тестом
  `test_defective_term_calculation_suspends_the_limitation_bar`.
- Версия пакета поднята до `0.91.0`; добавлены запись совместимости, шаг
  миграции `0.90.0 → 0.91.0`, фикстура `0.90.0-phase0-trace`, 89 отчётов
  `*-to-0.91.0`, шаг `evaluate-terms` в Phase 0 и спецификация
  `docs/contract-terms-spec.md`.
- QA: `ruff` — чисто; `pytest` — `970 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Понятие, виды и условия сделок, выпуск `0.90.0`

- Добавлен `src/causa/institutional/contracts/transactions.py` — формальная
  модель на Z3 по статьям 153–157.1 ГК РФ (понятие сделки, деление сделок по
  числу сторон, эффект односторонней сделки, применение к ней общих положений о
  договорах, сделки под отлагательным и отменительным условием, недобросовестное
  влияние на наступление условия, необходимость согласия на совершение сделки,
  порядок такого согласия, запрет считать молчание согласием).
- Сделки подключены к слою общих положений: `consent_missing_for_transaction`
  даёт вывод `transaction_challengeable_for_missing_consent` — сделка оспорима
  по статье 173.1 ГК РФ.
- Существенно то, чего слой **не** делает: он не снимает
  `contract_legally_effective` и не обесценивает договорные требования, потому
  что оспоримая сделка недействительна только в силу признания её таковой судом
  (пункт 1 статьи 166 ГК РФ). Это третий, отдельный способ входа общей части в
  анализ — наряду с вытеснением договора (статья 183) и отказом в судебной
  защите (статьи 10 и 199).
- Слой обновлён до
  `contracts-general-part-effects-articles-10-167-173-1-183-199-209-432-v4`.
- Проверено сквозным прогоном и закреплено тестом
  `test_missing_consent_makes_transaction_challengeable_without_voiding_it`,
  который проверяет именно отсутствие избыточного эффекта.
- Версия пакета поднята до `0.90.0`; добавлены запись совместимости, шаг
  миграции `0.89.0 → 0.90.0`, фикстура `0.89.0-phase0-trace`, 88 отчётов
  `*-to-0.90.0`, шаг `evaluate-transactions` в Phase 0 и спецификация
  `docs/contract-transactions-spec.md`.
- QA: `ruff` — чисто; `pytest` — `960 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Основные начала и защита гражданских прав, выпуск `0.89.0`

- Добавлен `src/causa/institutional/contracts/civil_principles.py` — формальная
  модель на Z3 по статьям 1–16.1 ГК РФ (добросовестность, равенство и свобода
  договора, основания возникновения прав, злоупотребление правом, отказ в защите
  права, способы защиты, самозащита, возмещение убытков, ответственность
  публично-правовых образований).
- Основные начала подключены к слою общих положений: `abuse_of_right_detected`
  даёт вывод `protection_refused_for_abuse` — в судебной защите отказано
  (пункт 2 статьи 10 ГК РФ), договорные требования неисполнимы, установленное
  нарушение не может быть положено в основание присуждения. Договор при этом
  сохраняет силу: злоупотребление правом не порочит сделку, а лишает защиты
  конкретное требование.
- Слой обновлён до
  `contracts-general-part-effects-articles-10-167-183-199-209-432-v3`: отказ в
  защите по злоупотреблению правом устроен параллельно отказу по исковой
  давности (`limitation_bars_protection` и `protection_refused_for_abuse`
  сходятся в `judicial_protection_available`).
- Проверено сквозным прогоном и закреплено тестом
  `test_abuse_of_right_refuses_protection_through_general_effects`.
- Версия пакета поднята до `0.89.0`; добавлены запись совместимости, шаг
  миграции `0.88.0 → 0.89.0`, фикстура `0.88.0-phase0-trace`, 87 отчётов
  `*-to-0.89.0`, шаг `evaluate-civil-principles` в Phase 0 и спецификация
  `docs/contract-civil-principles-spec.md`.
- QA: `ruff` — чисто; `pytest` — `950 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Право собственности и его защита, выпуск `0.88.0`

- Добавлен `src/causa/institutional/contracts/property_rights.py` — формальная
  модель на Z3 по статьям 209–305 ГК РФ (содержание права собственности,
  распоряжение неуправомоченным лицом, бремя содержания и риск, момент
  возникновения права, приобретательная давность, общая собственность,
  истребование имущества, защита добросовестного приобретателя, негаторная
  защита).
- Вещные права подключены к слою общих положений: `unauthorized_disposal_detected`
  даёт вывод `title_transfer_defeated` — титул к приобретателю не перешёл
  (статьи 209 и 302 ГК РФ). Договор при этом остаётся действительным, так как
  продажа чужой вещи сама по себе не влечёт недействительности.
- Слой обновлён до `contracts-general-part-effects-articles-167-183-199-209-432-v2`.
- Проверено сквозным прогоном и закреплено тестом
  `test_unauthorized_disposal_propagates_through_general_effects`.
- Версия пакета поднята до `0.88.0`; добавлены запись совместимости, шаг
  миграции `0.87.0 → 0.88.0`, фикстура `0.87.0-phase0-trace`, 86 отчётов
  `*-to-0.88.0`, шаг `evaluate-property-rights` в Phase 0 и спецификация
  `docs/contract-property-rights-spec.md`.
- QA: `ruff` — чисто; `pytest` — `940 passed`; benchmark `10/10`,
  red-team `10/10`.

### 2026-08-06 - Представительство и доверенность, выпуск `0.87.0`

- Добавлен `src/causa/institutional/contracts/representation.py` — формальная
  модель на Z3 по статьям 182–189 ГК РФ (квалификация представительства,
  основание полномочия, запрет сделок в отношении себя лично, коммерческое
  представительство, форма и срок доверенности, передоверие, прекращение и
  извещение, сделка неуполномоченного лица и эффект одобрения).
- Первый институт части первой ГК РФ, подключённый к слою общих положений, а не
  добавленный как остров: вывод `unauthorized_representation_detected` поступает
  в `general_effects` и по статье 183 ГК РФ снимает `contract_legally_effective`.
- Слой общих положений обновлён до версии
  `contracts-general-part-effects-articles-167-183-199-432-v1`.
- Проверено сквозным прогоном: при сделке неуполномоченного лица без одобрения
  `contract_legally_effective` False, `institute_conclusions_displaced` True,
  `breach_findings_without_effect` True, `requires_human_resolution` True.
  Регрессия закреплена тестом
  `test_unauthorized_representation_propagates_through_general_effects`.
- Версия пакета поднята до `0.87.0`; добавлены запись совместимости, шаг
  миграции `0.86.0 → 0.87.0`, фикстура `0.86.0-phase0-trace`, 85 отчётов
  `*-to-0.87.0`, шаг `evaluate-representation` в Phase 0 и спецификация
  `docs/contract-representation-spec.md`.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` — чисто;
  `pytest` — `930 passed`; benchmark `10/10`, red-team `10/10`.

### 2026-08-06 - Слой применения общих положений и выпуск `0.86.0`

- Проведена эмпирическая проверка: применяет ли система общие положения ГК к
  выводам специальных институтов. Установлено, что нет — при изменении фактов в
  моделях общей части (недействительность, форма, давность, добросовестность)
  число изменений в остальных 78 институтах равнялось нулю. Из 79 институтов
  67 не имели ни одной связи с другими; 35 существовавших межмодельных проверок
  были проверками согласованности входов, а не выводом.
- Добавлен `src/causa/institutional/contracts/general_effects.py` — первый узел
  пакета, который выводит, а не принимает утверждения рецензента. Входы слоя
  строятся из выводов якорных моделей: formation, invalidity, form, limitation,
  constraint, termination.
- Слой выводит действие договора как основания требований, доступность судебной
  защиты, вытеснение выводов специальных институтов, невозможность положить
  установленное нарушение в основание присуждения и применение последствий
  недействительности (статьи 167, 199 и 432 ГК РФ).
- Исправлен дефект: вывод модели статей 195–208 об истёкшей и заявленной
  исковой давности не влиял ни на один другой институт и не поднимал флаг
  экспертизы. Теперь распространяется; регрессия закреплена тестом
  `test_limitation_defense_propagates_to_whole_analysis`.
- `requires_human_general_effects_assessment` включён в дизъюнкцию
  `requires_human_resolution` всего анализа.
- Воспроизводимость проверяется дважды: входы из якорных оценок, оценка из
  входов. Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический
  артефакт, exporter, шаг `apply-general-part-effects` в Phase 0 и русская
  спецификация `docs/contract-general-effects-spec.md`.
- Версия пакета поднята до `0.86.0`; добавлены запись совместимости, шаг
  миграции `0.85.0 → 0.86.0`, фикстура `0.85.0-phase0-trace` и 84 отчёта
  `*-to-0.86.0`.
- Известное ограничение, зафиксированное в спецификации: согласованность фактов
  между институтами по-прежнему обеспечивается проверками входов, а не выводом —
  чтобы объявить сделку ничтожной, рецензент должен согласованно отразить это в
  данных нескольких институтов (каскад ручной согласованности).
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `pytest` — `920 passed`; benchmark `10/10`,
  red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Неосновательное обогащение и выпуск `0.85.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1102–1109 ГК РФ:
  `src/causa/institutional/contracts/unjust_enrichment.py` (установление
  неосновательного обогащения, обязанность его возврата, независимость правил
  от причин обогащения, применение правил к другим требованиям, возврат в
  натуре, возмещение стоимости, восстановление переданного права, возврат
  доходов и процентов, возмещение затрат на содержание имущества).
- Правила об имуществе, не подлежащем возврату, выводятся как зависимый
  признак — только при установленном нарушении обязанности возврата — и не
  включаются в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-unjust-enrichment`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-unjust-enrichment-spec.md`.
- Открыта и завершена глава 60 ГК РФ; часть вторая ГК РФ пройдена полностью.
- Версия пакета поднята до `0.85.0`; добавлены запись совместимости, шаг
  миграции `0.84.0 → 0.85.0`, фикстура `0.84.0-phase0-trace` и 83 отчёта
  `*-to-0.85.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.84.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `913 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Компенсация морального вреда и выпуск `0.84.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1099–1101 ГК РФ: `src/causa/institutional/contracts/moral_harm.py`
  (установление морального вреда, компенсация при посягательстве на
  нематериальные блага, пределы компенсации при нарушении имущественных прав,
  независимость от возмещения имущественного вреда, основания компенсации
  независимо от вины, вред источником повышенной опасности, незаконное
  привлечение к ответственности, распространение порочащих сведений, денежная
  форма и размер компенсации).
- Учёт индивидуальных особенностей потерпевшего выводится как зависимый
  признак — только при установленном нарушении способа или размера компенсации —
  и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-moral-harm`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-moral-harm-spec.md`.
- Завершена глава 59 ГК РФ «Обязательства вследствие причинения вреда».
- Версия пакета поднята до `0.84.0`; добавлены запись совместимости, шаг
  миграции `0.83.0 → 0.84.0`, фикстура `0.83.0-phase0-trace` и 82 отчёта
  `*-to-0.84.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.83.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `904 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Вред вследствие недостатков товаров и выпуск `0.83.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1095–1098 ГК РФ:
  `src/causa/institutional/contracts/product_liability.py` (установление вреда
  вследствие недостатка товара, работы или услуги, возмещение независимо от
  вины, требование о потребительской цели, выбор потерпевшим продавца или
  изготовителя, ответственность исполнителя, ответственность за
  непредоставление информации, сроки возмещения и основания освобождения от
  ответственности).
- Нарушение потребителем правил пользования или хранения выводится как
  зависимый признак — только при установленном нарушении оснований
  освобождения от ответственности — и не включается в дизъюнкцию признаков,
  требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-product-liability`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-product-liability-spec.md`.
- Продолжена глава 59 ГК РФ (§ 3).
- Версия пакета поднята до `0.83.0`; добавлены запись совместимости, шаг
  миграции `0.82.0 → 0.83.0`, фикстура `0.82.0-phase0-trace` и 81 отчёт
  `*-to-0.83.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.82.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `895 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Возмещение вреда жизни и здоровью и выпуск `0.82.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1084–1094 ГК РФ:
  `src/causa/institutional/contracts/tort_life_health.py` (установление вреда
  жизни или здоровью, объём и характер возмещения, расчёт утраченного
  заработка, правила о несовершеннолетнем потерпевшем, круг лиц и размер выплат
  по случаю смерти кормильца, изменение размера возмещения, порядок платежей и
  расходы на погребение).
- Индексация сумм возмещения выводится как зависимый признак — только при
  установленном нарушении правил об изменении размера возмещения — и не
  включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-tort-life-health`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-tort-life-health-spec.md`.
- Продолжена глава 59 ГК РФ (§ 2).
- Версия пакета поднята до `0.82.0`; добавлены запись совместимости, шаг
  миграции `0.81.0 → 0.82.0`, фикстура `0.81.0-phase0-trace` и 80 отчётов
  `*-to-0.82.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.81.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `886 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Общие положения о возмещении вреда и выпуск `0.81.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1064–1083 ГК РФ: `src/causa/institutional/contracts/tort_general.py`
  (установление причинения вреда, правило о полном возмещении, презумпция вины
  причинителя, необходимая оборона и крайняя необходимость, ответственность за
  вред, причинённый другими лицами, ответственность за источник повышенной
  опасности, солидарная ответственность и регресс, способ и размер возмещения,
  учёт вины потерпевшего).
- Уменьшение размера возмещения при грубой неосторожности потерпевшего
  выводится как зависимый признак — только при установленном нарушении учёта
  вины потерпевшего или имущественного положения причинителя — и не включается
  в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-tort-general`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-tort-general-spec.md`.
- Открыта глава 59 ГК РФ «Обязательства вследствие причинения вреда» (§ 1).
- Версия пакета поднята до `0.81.0`; добавлены запись совместимости, шаг
  миграции `0.80.0 → 0.81.0`, фикстура `0.80.0-phase0-trace` и 79 отчётов
  `*-to-0.81.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.80.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `877 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Институт проведения игр и пари и выпуск `0.80.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1062 и 1063 ГК РФ: `src/causa/institutional/contracts/games.py`
  (квалификация отношений из игр и пари, отказ в судебной защите, исключение
  для участников, действовавших под влиянием обмана, насилия или угрозы,
  защита требований из расчётных сделок, статус и разрешение организатора,
  форма договора с участником, правила проведения игр, объявленные условия о
  выигрыше и срок его выплаты).
- Право участника требовать выплаты выигрыша и возмещения убытков выводится
  как зависимый признак — только при установленном нарушении срока выплаты
  выигрыша — и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-games`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-games-spec.md`.
- Открыта глава 58 ГК РФ «Проведение игр и пари».
- Версия пакета поднята до `0.80.0`; добавлены запись совместимости, шаг
  миграции `0.79.0 → 0.80.0`, фикстура `0.79.0-phase0-trace` и 78 отчётов
  `*-to-0.80.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.79.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `868 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Публичное обещание награды, публичный конкурс и выпуск `0.79.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1055–1061 ГК РФ:
  `src/causa/institutional/contracts/public_promise.py` (квалификация
  публичного обещания награды и конкурса, требования к объявлению, размер и
  распределение награды, отмена обещания, условия конкурса и его общественно
  полезная цель, изменение и отмена конкурса, решение о выплате награды,
  возврат представленных работ).
- Невозмещение расходов отозвавшимся лицам выводится как зависимый признак —
  только при установленном нарушении правил об отмене публичного обещания
  награды — и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-public-promise`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-public-promise-spec.md`.
- Открыты главы 56 «Публичное обещание награды» и 57 «Публичный конкурс».
- Версия пакета поднята до `0.79.0`; добавлены запись совместимости, шаг
  миграции `0.78.0 → 0.79.0`, фикстура `0.78.0-phase0-trace` и 77 отчётов
  `*-to-0.79.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.78.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `859 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Институт простого товарищества и выпуск `0.78.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1041–1054 ГК РФ: `src/causa/institutional/contracts/partnership.py`
  (квалификация простого товарищества, состав сторон и цель совместной
  деятельности, вклады товарищей и общее имущество, ведение общих дел, право на
  информацию и распределение общих расходов, ответственность товарищей,
  распределение прибыли, прекращение договора и выход товарища, негласное
  товарищество).
- Ничтожность соглашения об устранении товарища от участия в прибыли выводится
  как зависимый признак — только при установленном нарушении правил о
  распределении прибыли — и не включается в дизъюнкцию признаков, требующих
  экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-partnership`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-partnership-spec.md`.
- Открыта глава 55 ГК РФ «Простое товарищество».
- Версия пакета поднята до `0.78.0`; добавлены запись совместимости, шаг
  миграции `0.77.0 → 0.78.0`, фикстура `0.77.0-phase0-trace` и 76 отчётов
  `*-to-0.78.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.77.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `850 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-05 - Институт коммерческой концессии и выпуск `0.77.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1027–1040 ГК РФ: `src/causa/institutional/contracts/franchise.py`
  (квалификация коммерческой концессии, объём предоставленных прав и состав
  сторон, письменная форма и государственная регистрация предоставления права,
  коммерческая субконцессия, вознаграждение, обязанности правообладателя и
  пользователя, ограничения прав сторон и ничтожные условия, ответственность
  правообладателя и прекращение договора).
- Ничтожность при пороке формы выводится как зависимый признак — только при
  установленном нарушении письменной формы или государственной регистрации —
  и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-franchise`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-franchise-spec.md`.
- Открыта глава 54 ГК РФ «Коммерческая концессия».
- Версия пакета поднята до `0.77.0`; добавлены запись совместимости, шаг
  миграции `0.76.0 → 0.77.0`, фикстура `0.76.0-phase0-trace` и 75 отчётов
  `*-to-0.77.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.76.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `841 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт доверительного управления и выпуск `0.76.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1012–1026 ГК РФ:
  `src/causa/institutional/contracts/trust_management.py` (квалификация
  доверительного управления, объект управления, статус управляющего,
  существенные условия и форма, обособление имущества, предупреждение об
  обременении, права и отчёт управляющего, его ответственность, вознаграждение
  и прекращение договора).
- Неприменение последствий несоблюдения формы выводится как зависимый признак —
  только при установленном нарушении существенных условий или формы договора —
  и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-trust-management`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-trust-management-spec.md`.
- Открыта глава 53 ГК РФ «Доверительное управление имуществом».
- Версия пакета поднята до `0.76.0`; добавлены запись совместимости, шаг
  миграции `0.75.0 → 0.76.0`, фикстура `0.75.0-phase0-trace` и 74 отчёта
  `*-to-0.76.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.75.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `832 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт агентирования и выпуск `0.75.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 1005–1011 ГК РФ: `src/causa/institutional/contracts/agency.py`
  (квалификация агентирования, сторона сделки агента, вознаграждение,
  допустимые ограничения прав сторон, ничтожные условия о покупателях, отчёты
  агента, возражения принципала, субагентский договор, прекращение договора и
  выбор применимых правил).
- Ничтожность условий об исключительном круге покупателей выводится как
  зависимый признак — только при установленном нарушении ограничений прав
  принципала и агента — и не включается в дизъюнкцию признаков, требующих
  экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-agency` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-agency-spec.md`.
- Открыта глава 52 ГК РФ «Агентирование».
- Версия пакета поднята до `0.75.0`; добавлены запись совместимости, шаг
  миграции `0.74.0 → 0.75.0`, фикстура `0.74.0-phase0-trace` и 73 отчёта
  `*-to-0.75.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.74.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `823 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт комиссии и выпуск `0.74.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 990–1004 ГК РФ: `src/causa/institutional/contracts/commission.py`
  (квалификация комиссии, вознаграждение и делькредере, указания комитента,
  уведомление об отступлении, ответственность за третье лицо, субкомиссия,
  права комитента на вещи, отчёт комиссионера, обязанности комитента и
  прекращение договора).
- Неуведомление об отступлении от указаний выводится как зависимый признак —
  только при установленном отступлении комиссионера от указаний комитента — и
  не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-commission` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-commission-spec.md`.
- Открыта глава 51 ГК РФ «Комиссия».
- Версия пакета поднята до `0.74.0`; добавлены запись совместимости, шаг
  миграции `0.73.0 → 0.74.0`, фикстура `0.73.0-phase0-trace` и 72 отчёта
  `*-to-0.74.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.73.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `814 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт действий в чужом интересе и выпуск `0.73.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 980–989 ГК РФ:
  `src/causa/institutional/contracts/negotiorum_gestio.py` (условия действий в
  чужом интересе, уведомление заинтересованного лица, ожидание его решения,
  последствия одобрения и неодобрения, возмещение расходов, вознаграждение,
  переход последствий сделки и отчёт).
- Нарушение обязанности выждать решение заинтересованного лица выводится как
  зависимый признак — только при установленном нарушении уведомления этого лица
  — и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-negotiorum-gestio`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-negotiorum-gestio-spec.md`.
- Открыта глава 50 ГК РФ «Действия в чужом интересе без поручения».
- Версия пакета поднята до `0.73.0`; добавлены запись совместимости, шаг
  миграции `0.72.0 → 0.73.0`, фикстура `0.72.0-phase0-trace` и 71 отчёт
  `*-to-0.73.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.72.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `805 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт поручения и выпуск `0.72.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 971–979 ГК РФ: `src/causa/institutional/contracts/mandate.py`
  (квалификация поручения, вознаграждение поверенного, указания доверителя,
  уведомление об отступлении, личное исполнение и передоверие, отчёт
  поверенного, обязанности доверителя, прекращение договора, его последствия и
  обязанности правопреемников).
- Неуведомление об отступлении от указаний выводится как зависимый признак —
  только при установленном отступлении поверенного от указаний доверителя — и
  не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-mandate` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-mandate-spec.md`.
- Открыта глава 49 ГК РФ «Поручение».
- Версия пакета поднята до `0.72.0`; добавлены запись совместимости, шаг
  миграции `0.71.0 → 0.72.0`, фикстура `0.71.0-phase0-trace` и 70 отчётов
  `*-to-0.72.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.71.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `796 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт исполнения страхового обязательства и выпуск `0.71.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 944–970 ГК РФ:
  `src/causa/institutional/contracts/insurance_settlement.py` (сообщение
  сведений при заключении договора, страховая сумма, страховая премия,
  увеличение страхового риска и досрочное прекращение, уведомление о страховом
  случае, уменьшение убытков, освобождение страховщика, суброгация и исковая
  давность).
- Неприменение последствий несвоевременного уведомления выводится как зависимый
  признак — только при установленном нарушении уведомления о страховом случае —
  и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-insurance-settlement`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-insurance-settlement-spec.md`.
- Завершена глава 48 ГК РФ «Страхование».
- Версия пакета поднята до `0.71.0`; добавлены запись совместимости, шаг
  миграции `0.70.0 → 0.71.0`, фикстура `0.70.0-phase0-trace` и 69 отчётов
  `*-to-0.71.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.70.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `787 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт страхования и выпуск `0.70.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 927–943 ГК РФ: `src/causa/institutional/contracts/insurance.py`
  (квалификация страхования, статус страховщика, страховой интерес, письменная
  форма, существенные условия, правила страхования, пределы имущественного и
  личного страхования, права выгодоприобретателя и обязательное страхование).
- Нарушение применения правил страхования выводится как зависимый признак —
  только при установленном несогласовании существенных условий в самом договоре
  — и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-insurance` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-insurance-spec.md`.
- Открыта глава 48 ГК РФ «Страхование».
- Версия пакета поднята до `0.70.0`; добавлены запись совместимости, шаг
  миграции `0.69.0 → 0.70.0`, фикстура `0.69.0-phase0-trace` и 68 отчётов
  `*-to-0.70.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.69.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `778 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт специальных видов хранения и выпуск `0.69.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 919–926 ГК РФ:
  `src/causa/institutional/contracts/special_storage.py` (квалификация
  специального вида хранения, хранение в ломбарде, хранение ценностей в банке,
  индивидуальный банковский сейф, камеры хранения транспортных организаций,
  невостребованные вещи, гардеробы организаций, вещи постояльца гостиницы,
  секвестр и пределы ответственности).
- Нарушение правил о невостребованных вещах выводится как зависимый признак —
  только при установленном нарушении хранения в камере хранения транспортной
  организации — и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-special-storage`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-special-storage-spec.md`.
- Завершена глава 47 ГК РФ «Хранение»: реализованы все три параграфа — общие
  положения, хранение на товарном складе и специальные виды хранения.
- Версия пакета поднята до `0.69.0`; добавлены запись совместимости, шаг
  миграции `0.68.0 → 0.69.0`, фикстура `0.68.0-phase0-trace` и 67 отчётов
  `*-to-0.69.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.68.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `769 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт хранения на товарном складе и выпуск `0.68.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 907–918 ГК РФ:
  `src/causa/institutional/contracts/warehouse_storage.py` (квалификация
  складского хранения, публичный характер договора склада общего пользования,
  осмотр товаров при приёме, фиксация расхождений, право товаровладельца на
  осмотр, уведомление об изменении условий хранения, проверка при возвращении,
  складские документы, двойное складское свидетельство и выдача товара).
- Нефиксация выявленных при приёме расхождений выводится как зависимый признак
  — только при установленном нарушении осмотра товаров при их приёме — и не
  включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-warehouse-storage`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-warehouse-storage-spec.md`.
- Продолжена глава 47 ГК РФ параграфом 2 «Хранение на товарном складе».
- Версия пакета поднята до `0.68.0`; добавлены запись совместимости, шаг
  миграции `0.67.0 → 0.68.0`, фикстура `0.67.0-phase0-trace` и 66 отчётов
  `*-to-0.68.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.67.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `760 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт хранения (общие положения) и выпуск `0.67.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 886–906 ГК РФ: `src/causa/institutional/contracts/storage.py`
  (квалификация хранения, письменная форма, обязанность принять вещь, срок
  хранения, меры по обеспечению сохранности, пользование вещью, уведомление об
  изменении условий хранения, вознаграждение и расходы, возврат вещи и
  ответственность хранителя).
- Нарушение правил об ответственности хранителя выводится как зависимый признак
  — только при установленном непринятии мер по обеспечению сохранности вещи — и
  не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-storage` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-storage-spec.md`.
- Открыта глава 47 ГК РФ «Хранение» параграфом 1 «Общие положения о хранении».
- Версия пакета поднята до `0.67.0`; добавлены запись совместимости, шаг
  миграции `0.66.0 → 0.67.0`, фикстура `0.66.0-phase0-trace` и 65 отчётов
  `*-to-0.67.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.66.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `751 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт расчётов и выпуск `0.66.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 861–885 ГК РФ: `src/causa/institutional/contracts/settlements.py`
  (безналичные расчёты, законность формы расчётов, исполнение платёжных
  поручений, условия и закрытие аккредитива, исполнение инкассовых поручений,
  реквизиты чека, его оплата и гарантия платежа авалем, удостоверение отказа от
  оплаты чека).
- Неприменение ответственности банка за неисполнение платёжного поручения
  выводится как зависимый признак — только при установленном нарушении
  исполнения поручения — и не включается в дизъюнкцию признаков, требующих
  экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-settlements` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-settlements-spec.md`.
- Открыта глава 46 ГК РФ «Расчёты».
- Версия пакета поднята до `0.66.0`; добавлены запись совместимости, шаг
  миграции `0.65.0 → 0.66.0`, фикстура `0.65.0-phase0-trace` и 64 отчёта
  `*-to-0.66.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.65.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `742 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт банковского счёта и выпуск `0.65.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 845–860 ГК РФ: `src/causa/institutional/contracts/bank_account.py`
  (квалификация банковского счёта, объявленные условия открытия, удостоверение
  прав распоряжения, сроки операций, ответственность банка за их ненадлежащее
  совершение, кредитование счёта, оплата услуг банка, списание без распоряжения
  клиента, банковская тайна и ограничение распоряжения, расторжение договора и
  возврат остатка).
- Неприменение ответственности банка за ненадлежащее совершение операций
  выводится как зависимый признак — только при установленном нарушении сроков
  операций по счёту — и не включается в дизъюнкцию признаков, требующих
  экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-bank-account` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-bank-account-spec.md`.
- Открыта глава 45 ГК РФ «Банковский счёт».
- Версия пакета поднята до `0.65.0`; добавлены запись совместимости, шаг
  миграции `0.64.0 → 0.65.0`, фикстура `0.64.0-phase0-trace` и 63 отчёта
  `*-to-0.65.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.64.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `733 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт банковского вклада и выпуск `0.64.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 834–844 ГК РФ: `src/causa/institutional/contracts/bank_deposit.py`
  (квалификация банковского вклада, право привлекать вклады, письменная форма,
  возврат вклада по первому требованию, проценты при досрочном возврате,
  выплата процентов, запрет одностороннего уменьшения ставки по срочному вкладу
  гражданина, обеспечение возврата, вклады третьих лиц и в пользу третьих лиц,
  сберегательная книжка и сертификат).
- Нарушение расчёта процентов при досрочном возврате выводится как зависимый
  признак — только при установленном нарушении возврата вклада по первому
  требованию — и не включается в дизъюнкцию признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-bank-deposit` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-bank-deposit-spec.md`.
- Открыта глава 44 ГК РФ «Банковский вклад».
- Версия пакета поднята до `0.64.0`; добавлены запись совместимости, шаг
  миграции `0.63.0 → 0.64.0`, фикстура `0.63.0-phase0-trace` и 62 отчёта
  `*-to-0.64.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.63.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `724 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт финансирования под уступку денежного требования и выпуск `0.63.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 824–833 ГК РФ: `src/causa/institutional/contracts/factoring.py`
  (квалификация договора факторинга, определённость уступаемого требования,
  статус финансового агента, договорный запрет уступки, ответственность
  клиента за действительность требования, последующая уступка, уведомление
  должника, зачёт встречных требований должника, расчёты агента с клиентом и
  адресат требования должника о возврате сумм).
- Право должника на зачёт выводится как зависимый признак — только при
  установленном нарушении уведомления должника — и не включается в дизъюнкцию
  признаков, требующих экспертизы.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-factoring` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-factoring-spec.md`.
- Открыта глава 43 ГК РФ «Финансирование под уступку денежного требования».
- Версия пакета поднята до `0.63.0`; добавлены запись совместимости, шаг
  миграции `0.62.0 → 0.63.0`, фикстура `0.62.0-phase0-trace` и 61 отчёт
  `*-to-0.63.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.62.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `715 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт товарного и коммерческого кредита и выпуск `0.62.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 822 и 823 ГК РФ:
  `src/causa/institutional/contracts/commercial_credit.py` (квалификация
  товарного кредита, предоставление вещей, условия о количестве и качестве,
  применение правил о займе, квалификация коммерческого кредита, согласование
  его условий, проценты, пределы применения правил главы и установленный
  законом запрет).
- Модель содержит два независимых квалификатора — товарного и коммерческого
  кредита; последующие признаки выводятся только при соответствующей
  квалификации.
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-commercial-credit`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-commercial-credit-spec.md`.
- Завершена глава 42 ГК РФ «Заём и кредит»: реализованы все три параграфа —
  заём, кредит и товарный и коммерческий кредит.
- Версия пакета поднята до `0.62.0`; добавлены запись совместимости, шаг
  миграции `0.61.0 → 0.62.0`, фикстура `0.61.0-phase0-trace` и 60 отчётов
  `*-to-0.62.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.61.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `706 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-04 - Институт кредита и выпуск `0.61.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 819–821.1 ГК РФ: `src/causa/institutional/contracts/credit.py`
  (квалификация кредитного договора, статус кредитора, проценты и иные платежи,
  режим потребительского кредита, письменная форма под страхом ничтожности,
  отказ кредитора, уведомление заёмщика об отказе, контроль за целевым
  кредитом, требование досрочного возврата и ограничение для гражданина).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-credit` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `citizen_early_repayment_restriction_breached` не добавляет отдельного
  основания для экспертизы: соответствующий случай уже охвачен признаком
  `early_repayment_demand_unjustified`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-credit-spec.md`.
- Продолжена глава 42 ГК РФ «Заём и кредит» параграфом 2 «Кредит».
- Версия пакета поднята до `0.61.0`; добавлены запись совместимости, шаг
  миграции `0.60.0 → 0.61.0`, фикстура `0.60.0-phase0-trace` и 59 отчётов
  `*-to-0.61.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.60.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `697 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-03 - Институт займа и выпуск `0.60.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 807–818 ГК РФ: `src/causa/institutional/contracts/loan.py`
  (квалификация займа, письменная форма, правила о процентах и беспроцентном
  займе, уменьшение ростовщических процентов, обязанность возврата, проценты за
  просрочку, оспаривание по безденежности, утрата обеспечения, контроль за
  целевым займом и новация долга в заёмное обязательство).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-loan` в Phase 0 и
  ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `late_payment_interest_due` не добавляет отдельного основания для
  экспертизы: соответствующий случай уже охвачен признаком
  `repayment_duty_breached`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-loan-spec.md`.
- Открыта глава 42 ГК РФ «Заём и кредит» параграфом 1 «Заём».
- Версия пакета поднята до `0.60.0`; добавлены запись совместимости, шаг
  миграции `0.59.0 → 0.60.0`, фикстура `0.59.0-phase0-trace` и 58 отчётов
  `*-to-0.60.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.59.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `688 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-03 - Институт транспортной экспедиции и выпуск `0.59.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 801–806 ГК РФ: `src/causa/institutional/contracts/forwarding.py`
  (квалификация экспедиции, письменная форма и доверенность, выполнение
  экспедиционных услуг, ответственность, связанная с договором перевозки,
  предоставление клиентом документов и информации, сообщение экспедитора о
  неполноте сведений, привлечение третьих лиц, предупреждение об отказе в
  разумный срок, возмещение убытков от расторжения и штраф).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-forwarding` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признаки `forwarder_notice_duty_breached` и `withdrawal_losses_compensation_due`
  не добавляют отдельных оснований для экспертизы: соответствующие случаи уже
  охвачены признаками `client_information_duty_breached` и
  `withdrawal_notice_duty_breached`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-forwarding-spec.md`.
- Открыта глава 41 ГК РФ «Транспортная экспедиция».
- Версия пакета поднята до `0.59.0`; добавлены запись совместимости, шаг
  миграции `0.58.0 → 0.59.0`, фикстура `0.58.0-phase0-trace` и 57 отчётов
  `*-to-0.59.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.58.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `679 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-02 - Институт перевозки и выпуск `0.58.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 784–800 ГК РФ: `src/causa/institutional/contracts/carriage.py`
  (квалификация перевозки, транспортные документы, отказ перевозчика общего
  пользования, провозная плата и удержание груза, подача транспортных средств,
  сроки доставки, задержка отправления пассажира, утрата и повреждение груза,
  ответственность перевозчика и недействительность соглашений об ограничении
  ответственности).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-carriage` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `carrier_liable_for_cargo_loss` не добавляет отдельного основания для
  экспертизы: соответствующий случай уже охвачен признаком
  `cargo_damage_established`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-carriage-spec.md`.
- Открыта глава 40 ГК РФ «Перевозка».
- Версия пакета поднята до `0.58.0`; добавлены запись совместимости, шаг
  миграции `0.57.0 → 0.58.0`, фикстура `0.57.0-phase0-trace` и 56 отчётов
  `*-to-0.58.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.57.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `670 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-02 - Институт возмездного оказания услуг и выпуск `0.57.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 779–783.1 ГК РФ: `src/causa/institutional/contracts/paid_services.py`
  (квалификация договора, исключение услуг по договорам отдельных глав Кодекса,
  личное исполнение, сроки и порядок оплаты, полная оплата при невозможности по
  вине заказчика, возмещение фактически понесённых расходов при невозможности
  без вины сторон, отказ заказчика, отказ исполнителя и правила приостановления
  услуг связи).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-paid-services` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Невозможность исполнения по вине заказчика и невозможность по обстоятельствам,
  за которые ни одна из сторон не отвечает, взаимно исключаются на уровне
  проверки согласованности фактов.
- Признак `actual_expenses_reimbursement_due` не добавляет отдельного основания
  для экспертизы: соответствующий случай уже охвачен признаком
  `no_fault_impossibility_established`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-paid-services-spec.md`.
- Открыта глава 39 ГК РФ «Возмездное оказание услуг».
- Версия пакета поднята до `0.57.0`; добавлены запись совместимости, шаг
  миграции `0.56.0 → 0.57.0`, фикстура `0.56.0-phase0-trace` и 55 отчётов
  `*-to-0.57.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.56.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `661 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-02 - Институт научно-исследовательских работ и выпуск `0.56.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 769–778 ГК РФ: `src/causa/institutional/contracts/research_work.py`
  (квалификация договора, личное проведение исследований, конфиденциальность
  сведений, пределы и условия использования результатов, гарантия ненарушения
  исключительных прав других лиц, сообщение о невозможности получить результат,
  обязанности заказчика, невозможность достижения результата без вины
  исполнителя, оплата работ до выявления невозможности и ответственность
  исполнителя).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-research-work` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `pre_impossibility_payment_due` не добавляет отдельного основания для
  экспертизы: соответствующий случай уже охвачен признаком
  `impossibility_without_fault_established`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-research-work-spec.md`.
- Открыта глава 38 ГК РФ «Выполнение научно-исследовательских,
  опытно-конструкторских и технологических работ» после завершённой главы 37.
- Версия пакета поднята до `0.56.0`; добавлены запись совместимости, шаг
  миграции `0.55.0 → 0.56.0`, фикстура `0.55.0-phase0-trace` и 54 отчёта
  `*-to-0.56.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.55.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `652 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-08-02 - Институт подрядных работ для государственных нужд и выпуск `0.55.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 763–768 ГК РФ: `src/causa/institutional/contracts/state_work.py`
  (квалификация работ для государственных или муниципальных нужд, требование
  государственного или муниципального контракта, статус заказчика как
  получателя бюджетных средств, порядок заключения контракта, условия об объёме
  и стоимости работ, сроках начала и окончания, финансировании и оплате,
  способах обеспечения исполнения обязательств, согласование новых условий при
  уменьшении бюджетных средств и возмещение убытков подрядчика).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-state-work` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `contractor_losses_compensation_due` не добавляет отдельного основания
  для экспертизы: соответствующий случай уже охвачен признаком
  `budget_reduction_terms_not_agreed`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-state-work-spec.md`.
- Завершена глава 37 ГК РФ «Подряд»: реализованы все пять параграфов — общие
  положения, бытовой подряд, строительный подряд, проектные и изыскательские
  работы и подрядные работы для государственных нужд.
- Версия пакета поднята до `0.55.0`; добавлены запись совместимости, шаг
  миграции `0.54.0 → 0.55.0`, фикстура `0.54.0-phase0-trace` и 53 отчёта
  `*-to-0.55.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.54.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `643 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-31 - Институт проектных и изыскательских работ и выпуск `0.54.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 758–762 ГК РФ: `src/causa/institutional/contracts/design_work.py`
  (квалификация договора, передача задания и исходных данных, отступление от
  требований задания без согласия заказчика, согласование документации с
  заказчиком и компетентными органами, запрет передачи документации третьим
  лицам, право третьих лиц воспрепятствовать работам, ответственность за
  недостатки документации, их выявление в ходе строительства или эксплуатации,
  оплата и содействие заказчика и возмещение дополнительных расходов).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-design-work` в Phase 0
  и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `later_discovered_defect_claim` не добавляет отдельного основания для
  экспертизы: соответствующий случай уже охвачен признаком
  `designer_liable_for_defects`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-design-work-spec.md`.
- Продолжена глава 37 ГК РФ «Подряд» параграфом 4 «Подряд на выполнение
  проектных и изыскательских работ».
- Версия пакета поднята до `0.54.0`; добавлены запись совместимости, шаг
  миграции `0.53.0 → 0.54.0`, фикстура `0.53.0-phase0-trace` и 52 отчёта
  `*-to-0.54.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.53.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `634 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-31 - Институт строительного подряда и выпуск `0.53.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 740–757 ГК РФ:
  `src/causa/institutional/contracts/construction_contract.py` (квалификация
  строительного подряда, страхование риска случайной гибели объекта,
  согласование технической документации и сметы, сообщение заказчику о не
  учтённых работах, предоставление участка и услуг, контроль заказчика за ходом
  работ, расчёты при консервации, односторонний акт приёмки, отступления от
  документации и обязательных норм и предельный пятилетний срок обнаружения
  недостатков).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-construction-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `five_year_defect_claim_available` не добавляет отдельного основания
  для экспертизы: соответствующий случай уже охвачен признаком
  `construction_quality_breached`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-construction-contract-spec.md`.
- Продолжена глава 37 ГК РФ «Подряд» параграфом 3 «Строительный подряд».
- Версия пакета поднята до `0.53.0`; добавлены запись совместимости, шаг
  миграции `0.52.0 → 0.53.0`, фикстура `0.52.0-phase0-trace` и 51 отчёт
  `*-to-0.53.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.52.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `625 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-31 - Институт бытового подряда и выпуск `0.52.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 730–739 ГК РФ: `src/causa/institutional/contracts/consumer_work.py`
  (квалификация бытового подряда, запрет навязывать дополнительную работу,
  право заказчика прекратить договор до сдачи работы, обязанность сообщить
  информацию о работе, недоброкачественный материал подрядчика, порядок оплаты
  после сдачи работы, сведения об использовании результата, существенные
  недостатки, десятилетний срок их обнаружения и двухмесячное предупреждение
  перед продажей невостребованного результата).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-consumer-work`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `ten_year_claim_available` не добавляет отдельного основания для
  экспертизы: соответствующий случай уже охвачен признаком
  `significant_defect_remedy_available`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-consumer-work-spec.md`.
- Продолжена глава 37 ГК РФ «Подряд» параграфом 2 «Бытовой подряд».
- Версия пакета поднята до `0.52.0`; добавлены запись совместимости, шаг
  миграции `0.51.0 → 0.52.0`, фикстура `0.51.0-phase0-trace` и 50 отчётов
  `*-to-0.52.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.51.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `616 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-31 - Институт подряда (общие положения) и выпуск `0.51.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 702–729 ГК РФ: `src/causa/institutional/contracts/work_contract.py`
  (квалификация подряда, обязанность выполнить работу лично, согласование
  начального и конечного сроков, предупреждение о существенном превышении
  сметы, непригодность материала заказчика, предупреждение об обстоятельствах,
  угрожающих годности работы, ответственность за недостатки результата, срок их
  обнаружения, обязанность осмотреть и принять результат и оплата выполненной
  части при одностороннем отказе заказчика).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-work-contract`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Признак `defect_claim_within_period` не добавляет отдельного основания для
  экспертизы: соответствующий случай уже охвачен признаком
  `contractor_liable_for_defects`.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-work-contract-spec.md`.
- Открыта глава 37 ГК РФ «Подряд» параграфом 1 «Общие положения о подряде».
- Версия пакета поднята до `0.51.0`; добавлены запись совместимости, шаг
  миграции `0.50.0 → 0.51.0`, фикстура `0.50.0-phase0-trace` и 49 отчётов
  `*-to-0.51.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.50.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check src tests
  scripts` — чисто; `git diff --check` — чисто; `pytest` — `607 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-29 - Закрытие форматного техдолга и усиление CI

- `ruff format` применён ко всей кодовой базе: 28 файлов, накопивших расхождение
  с каноническим форматом, приведены к нему; 316 файлов `src`, `tests` и
  `scripts` полностью соответствуют `ruff format --check`.
- Затронуты только пробелы и переносы строк: все 874 артефакта в `examples/`
  после полной перегенерации побайтово идентичны состоянию до форматирования
  (сверка по sha256), `pytest` — `598 passed` без изменений.
- CI усилен, чтобы расхождение не возникло снова:
  `ruff check` расширен с `src tests` до `src tests scripts`, добавлен
  отдельный шаг `ruff format --check src tests scripts`.
- Версия пакета не менялась: изменения не затрагивают юридические модели,
  схемы данных и выводы, поэтому это обслуживание, а не семантический выпуск.

### 2026-07-28 - Институт безвозмездного пользования и выпуск `0.50.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 689–701 ГК РФ: `src/causa/institutional/contracts/gratuitous_use.py`
  (квалификация, запрет передачи вещи инсайдеру организации, предоставление
  вещи с принадлежностями, ответственность за умышленно скрытые недостатки,
  права третьих лиц, содержание вещи, риск случайной гибели, досрочное
  расторжение, месячный срок извещения и сохранение прав при отчуждении).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-gratuitous-use`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Ссуда — безвозмездный аналог аренды; запрет передачи инсайдеру проверяется
  только при квалификации отношений как безвозмездного пользования.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-gratuitous-use-spec.md`.
- Открыта глава 36 ГК РФ «Безвозмездное пользование».
- Версия пакета поднята до `0.50.0`; добавлены запись совместимости, шаг
  миграции `0.49.0 → 0.50.0`, фикстура `0.49.0-phase0-trace` и 48 отчётов
  `*-to-0.50.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.49.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `598 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт найма жилого помещения и выпуск `0.49.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 671–688 ГК РФ: `src/causa/institutional/contracts/residential_lease.py`
  (квалификация, письменная форма, пригодность и изолированность помещения,
  обязанности наймодателя, нарушения нанимателя, одностороннее изменение платы,
  преимущественное право на новый срок, судебный порядок расторжения и срок
  для устранения нарушения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-residential-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Преимущественное право не применяется к краткосрочному найму до одного года;
  срок для устранения проверяется только при нарушении нанимателя.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация
  `docs/contract-residential-lease-spec.md`.
- Открыта глава 35 ГК РФ «Наём жилого помещения» после завершённой главы 34.
- Версия пакета поднята до `0.49.0`; добавлены запись совместимости, шаг
  миграции `0.48.0 → 0.49.0`, фикстура `0.48.0-phase0-trace` и 47 отчётов
  `*-to-0.49.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.48.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `589 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт финансовой аренды и выпуск `0.48.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 665–670 ГК РФ: `src/causa/institutional/contracts/leasing.py`
  (квалификация, допустимость предмета лизинга, уведомление продавца,
  непередача предмета по вине лизингодателя, переход риска в момент передачи,
  прямые требования арендатора к продавцу и солидарная ответственность).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-leasing`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Солидарная ответственность проверяется только при выборе продавца
  лизингодателем; ответственность за просрочку — только при самой просрочке.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-leasing-spec.md`.
- Выпуск завершает главу 34 ГК РФ «Аренда»: общие положения (`0.43.0`), прокат
  (`0.44.0`), транспортные средства (`0.45.0`), здания и сооружения (`0.46.0`),
  предприятия (`0.47.0`) и финансовая аренда (`0.48.0`).
- Версия пакета поднята до `0.48.0`; добавлены запись совместимости, шаг
  миграции `0.47.0 → 0.48.0`, фикстура `0.47.0-phase0-trace` и 46 отчётов
  `*-to-0.48.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.47.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `580 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт аренды предприятий и выпуск `0.47.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 656–664 ГК РФ: `src/causa/institutional/contracts/enterprise_lease.py`
  (квалификация, форма одного документа, государственная регистрация,
  уведомление кредиторов и согласие на перевод долгов, акт передачи и
  подготовка за счёт арендодателя, право распоряжения ценностями, содержание
  предприятия и подготовка к возврату за счёт арендатора).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-enterprise-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Аренда предприятия — аренда имущественного комплекса для предпринимательской
  деятельности; перевод долгов проверяется только при такой квалификации.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-enterprise-lease-spec.md`.
- Версия пакета поднята до `0.47.0`; добавлены запись совместимости, шаг
  миграции `0.46.0 → 0.47.0`, фикстура `0.46.0-phase0-trace` и 45 отчётов
  `*-to-0.47.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.46.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `571 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-28 - Институт аренды зданий и сооружений и выпуск `0.46.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 650–655 ГК РФ: `src/causa/institutional/contracts/building_lease.py`
  (квалификация, форма одного документа, государственная регистрация при сроке
  не менее года, права на земельный участок, сохранение права пользования при
  смене собственника, существенное условие о плате, акты передачи и возврата).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-building-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Регистрация проверяется только при сроке не менее года, сохранение права
  пользования участком — только при смене его собственника.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-building-lease-spec.md`.
- Версия пакета поднята до `0.46.0`; добавлены запись совместимости, шаг
  миграции `0.45.0 → 0.46.0`, фикстура `0.45.0-phase0-trace` и 44 отчёта
  `*-to-0.46.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.45.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `562 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-27 - Институт аренды транспортных средств и выпуск `0.45.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 632–649 ГК РФ: `src/causa/institutional/contracts/vehicle_lease.py`
  (квалификация, письменная форма, преимущественное право, содержание и ремонт,
  услуги экипажа, расходы по эксплуатации, страхование, субаренда и
  ответственность за вред третьим лицам).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-vehicle-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Аренда транспортных средств делится на аренду с экипажем (статьи 632–641) и
  без экипажа (статьи 642–649); признак вида договора определяет сторону,
  несущую соответствующую обязанность.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-vehicle-lease-spec.md`.
- Версия пакета поднята до `0.45.0`; добавлены запись совместимости, шаг
  миграции `0.44.0 → 0.45.0`, фикстура `0.44.0-phase0-trace` и 43 отчёта
  `*-to-0.45.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.44.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `553 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-27 - Институт проката и выпуск `0.44.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 626–631 ГК РФ: `src/causa/institutional/contracts/rental.py`
  (квалификация проката, письменная форма, предельный срок, преимущественное
  право, недостатки и их устранение, возврат части платы, ремонт и запрет
  распоряжения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-rental`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Прокат — вид аренды с публичным договором для потребительских целей (§2 главы 34).
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-rental-spec.md`.
- Версия пакета поднята до `0.44.0`; добавлены запись совместимости, шаг
  миграции `0.43.0 → 0.44.0`, фикстура `0.43.0-phase0-trace` и 42 отчёта
  `*-to-0.44.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.43.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `544 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-27 - Институт аренды (общие положения) и выпуск `0.43.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 606–625 ГК РФ: `src/causa/institutional/contracts/lease.py`
  (квалификация аренды, определённость объекта, форма и регистрация,
  ответственность за недостатки, права третьих лиц, субаренда, капитальный
  ремонт, расторжение, преимущественное право и улучшения).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-lease`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Аренда — возмездный договор о передаче имущества во временное владение и
  пользование (глава 34), открывающий крупную главу об аренде.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-lease-spec.md`.
- Версия пакета поднята до `0.43.0`; добавлены запись совместимости, шаг
  миграции `0.42.0 → 0.43.0`, фикстура `0.42.0-phase0-trace` и 41 отчёт
  `*-to-0.43.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.42.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `535 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.

### 2026-07-26 - Институт ренты и пожизненного содержания и выпуск `0.42.0`

- Добавлен отдельный проверенный контракт данных и формальная модель на Z3 по
  статьям 583–605 ГК РФ: `src/causa/institutional/contracts/annuity.py`
  (квалификация ренты, нотариальная форма, обеспечение и проценты за просрочку,
  выкуп постоянной ренты, расторжение пожизненной ренты и обременение имущества
  пожизненного содержания).
- Институт интегрирован в reviewed analysis с проверками схемы, источников,
  `case_id` и воспроизводимости; добавлен шаг `evaluate-annuity`
  в Phase 0 и ссылки в readiness (`ws4`, `ws8`, `ws9`).
- Рента и пожизненное содержание — возмездный алеаторный договор главы 33.
- Добавлены benchmark (`10/10`), red-team (`10/10`), синтетический артефакт и
  exporter; создана русская спецификация `docs/contract-annuity-spec.md`.
- Версия пакета поднята до `0.42.0`; добавлены запись совместимости, шаг
  миграции `0.41.0 → 0.42.0`, фикстура `0.41.0-phase0-trace` и 40 отчётов
  `*-to-0.42.0`; прежние отчёты `*-to-0.17.0` … `*-to-0.41.0` сохранены как
  исторические.
- QA: `ruff check src tests scripts` — чисто; `ruff format --check` файлов
  релиза — чисто; `git diff --check` — чисто; `pytest` — `526 passed`;
  benchmark `10/10`, red-team `10/10`; артефакты и миграции воспроизводимы.
